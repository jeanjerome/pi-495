story_id: e25s04
verified_at: "2026-09-24T17:30:42Z"
verifier: verify-work
risk: P1
branch: modele-choisi-admis
revision: 535ea23
revision_note: >
  The review, five rounds with two reviewers each, closed its gate on 535ea23 with no must-fix or
  should-fix left. Rounds 4 and 5 reviewed the fix of BUG-2026-09-24T175500, decided by the owner
  after round 3: both reviewers found the fix sound and asked for the audit and security review to
  cover it, and for the terminal-interface observation to be replayed with the commands it names;
  done in 535ea23. The RPC campaigns ran before the fix, which does not touch the channel they
  measure.

  Round 1 to 3, closed on a888f48: Round 1 on c439559: D-61 said the cost line names the model reached, which the
  code does not do (must-fix, both reviewers); the security claims said the owner's home path was
  absent from the dossiers, where workspace paths carry it; D-46 and D-57 still promised a
  destination judged before a request leaves; an AGT-07 row was added to the archived [P0] matrix.
  All fixed in 73a7068, amended to 80ea142 for its subject. Round 2: the no-secret claim had widened
  to a file holding a pre-existing loopback address, and a commit subject cited decision ids; fixed.
  The TUI double notice, which the branch neither introduces nor makes reachable, is logged as
  BUG-2026-09-24T175500. Round 3 passed with two consider items, both applied.

  Before, on c96a05d:
  The branch changes no production code: src/, contracts/, dist/ and package.json are those of main
  at 3b64971. It writes D-61, marks D-53 moot, updates the traceability matrix, and records three
  real runs in specs/verifications/e25s04-modele-choisi-admis.md. The runs used the dist/ built at
  698da99, byte-identical in its sources to main.

environment:
  pi_version: "0.87.1"
  node: "24.21.0"
  sandbox: "seatbelt, qualified"
  pi_configuration: "the owner's own, no PI_CODING_AGENT_DIR; default model omlx/qwen3.8-27b-oq8e"
  environment_digest: "sha256:d2dc09529ebb30cbc91f0c6b91682591e89dab500c758b6b476a72f07316b928, the same in the three dossiers"

phases:
  smoke:
    passed: true
    note: "Cold start of the built extension from dist/ in a fresh data directory for each run, through a real `pi -ne --mode rpc --no-session`."
  build:
    passed: true
    command: "npm run build"
    exit_code: 0
  typecheck:
    passed: true
    note: "Runs inside npm run check."
  lint:
    passed: true
    note: "The eight controls chained in npm run check; lint:traceability reports 85 + 8 [P0] requirements present."
  tests:
    passed: true
    count: 455

terminal_verdict:
  command: "npm run build && npm run check"
  exit_code: 0
  started_at: "2026-09-24T18:21:59Z"
  ended_at: "2026-09-24T18:24:21Z"
  note: "Single run under Node 24.21.0 on 535ea23, working tree clean. 455 tests passing, 0 failing. The same command exited 0 on a888f48 (452 tests), 72b5640 and c96a05d."

task_verify_commands:
  note: "Each task's own verify command, under Node 24.21.0; every one exits 0."
  results:
    - { task: 1, command: "test -n \"$(find specs/adr -name 'D-61-*.md')\"", exit_code: 0 }
    - { task: 2, command: "grep -q 'e25s04-negatif' specs/verifications/e25s04-modele-choisi-admis.md", exit_code: 0 }
    - { task: 3, command: "npm run lint:traceability", exit_code: 0 }
    - { task: 4, command: "npm run check", exit_code: 0, tests: 455 }

security:
  passed: true
  findings_at_or_above_confidence_8: 0
  report: "specs/security/REVIEW.md — section e25s04"
  note: >
    Task 2's security_verify is established: the record reproduces no provider address and no token.
    The files the branch changes were searched for the local model's address and key, the
    Anthropic address, the subscription's access and refresh tokens, read from the owner's
    configuration without being printed, and the owner's home path: 0 hits in the lines the branch
    adds. One hit predates the branch: the local model's loopback address, cited by the e25s03 section
    of specs/security/REVIEW.md. The three data
    directories were searched for the addresses, the key, the tokens and the witness name: 0 hits
    outside the driver's own output. They do carry the owner's home path, in the absolute paths of
    their workspaces, which were not searched for.

blind_spots:
  ran: false
  note: "Same derogation as e25s05: check-blind-spots.sh cannot match its state table, so its verdict is not evidence."

completeness_critic:
  ran: false
  note: "Same derogation as e25s01 to e25s05: it looks for story identifiers in the sources, which the written-content rules forbid."

manual:
  method: >
    Three runs through a real `pi --mode rpc` under the owner's Pi configuration, driven by
    ~/.495-campagnes/scripts/e25s04-drive.ts and run one after the other by e25s04-run.sh, each on its
    own data directory and its own clone of the minimal JS target. The screen is read from the
    `notify` requests Pi's RPC mode forwards, the structured entry from the `495` messages, and the
    dossier read-only from SQLite, the object store and the export.
  confirmed_by: "owner, 2026-09-24"
  runs:
    - id: "e25s04-local"
      observed: "Accepted, G0..G5 PASS. Three starts journaled omlx/qwen3.8-27b-oq8e on_machine. No notice on the screen, no model message on the structured entry."
    - id: "e25s04-distant"
      observed: >
        anthropic/claude-sonnet-5 selected under half a second after specify started. One warning on the screen at the
        selection, the same announcement once on the structured entry, without the address. specify
        journaled omlx on_machine; prepare and implement anthropic/claude-sonnet-5 off_machine, their
        requests observed as anthropic-messages carrying the subscription block. Accepted, G0..G5
        PASS; 0.0758 USD at the host catalogue's rates, through the subscription.
    - id: "e25s04-negatif"
      observed: >
        A policy.egress list naming only temoin-liste-e25s04 is refused at opening and on each /495,
        "policy.egress is no longer read"; no change created. The key removed and Pi reloaded, the
        change is created after the reload and accepted, G0..G5 PASS, with no refusal. The witness
        name is nowhere in the data directory, outside the driver's own output, nor in what Pi
        received.
  defects_found:
    - >
      BUG-2026-09-24T175500 (specs/bugs/registry.yaml), fixed by the owner's decision: in the TUI,
      `emit` doubled every structured message with an info notice, so the queued off-machine
      announcement, already said as a warning, was said again on the screen with the next `/495`, and
      every opening diagnostic likewise. Red 447df46 (2 failures of 2 in
      test/v3/model-select.test.ts), green 3a9ef82; a third test for an ordinary opening diagnostic,
      red on a888f48's code, green at 4591fe8. Seen in Pi's real terminal interface through a
      pseudo-terminal and a notice-recording probe extension (~/.495-campagnes/e25s04-tui), opened
      on a non-loopback model, then `/495 status` twice: warning, the same text as info, then the two
      replies on a888f48's build; warning alone, then the two replies, on the branch's build. A first
      pass typed too fast and ran `/495` once, answered with help; it is kept as avant-1.log and
      apres-1.log. The RPC campaigns ran before the fix, which does
      not touch the channel they measure.
  deviations:
    - >
      The specification expected the cost line of the Anthropic interventions to name the model. The
      cost line names a model only when its cost is unknown; the model reached is established by the
      imposed layer observed in the request instead. The specification (§12, §14, §17) is corrected
      to say so after the owner confirmed the acceptance; the owner is told of the corrected criterion
      when the review closes.
  agt_07:
    note: >
      AGT-07 is [P1] and has no row in the archived matrix, which carries [P0] requirements only.
      Its evidence: the model is read at each intervention and the boundary journaled (e25s04-distant:
      specify under omlx, prepare and implement under anthropic/claude-sonnet-5), the cost recorded
      when known and marked unknown otherwise (the three dossiers), and no fallback from one provider
      to another exists (a model Pi does not know fails the intervention, "no fallback is attempted",
      src/adapters/pi-worker/worker-main.ts).
      Tests: test/v3/model-select.test.ts, test/v3/session-cost.test.ts, test/v2/measure-budgets.test.ts.
  feigned:
    - "`/model` is the RPC command `set_model`, which calls the same `session.setModel`."
    - "`/reload` is Pi's example command `/reload-runtime`, which calls the same `session.reload()`."
    - "The extension is loaded through the owner's package entry, which points at the same dist/ file as the explicit -e."
    - "The TUI is not driven: the screen measured is the notify warning Pi's RPC mode forwards."
