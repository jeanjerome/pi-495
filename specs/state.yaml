workflow_mode: solo-git

project_cycle:
  current_phase: 6
  phase_name: release
  completed_phases: [1, 2, 3, 4, 5]

active_flow: quality-referential
active_epic_id: null

handoff:
  next_skill: release-branch
  last_step_completed: "Biome holds the harness: lint baseline cleared to zero, formatter adopted at 120 columns, both refused by Preflight"
  open_decisions:
    - "Import order stays grouped by layer rather than machine-sorted"
    - "QLT-01..05 and ARC-01..03 remain absent from the kernel: this adoption covers the harness code only"
  required_reading:
    - docs/DECISIONS.md (D-42, D-43)
    - biome.jsonc

git:
  branch: main
  hash: 51ed57f6f4fee17e0a6af87e5694774dd15b6f55
