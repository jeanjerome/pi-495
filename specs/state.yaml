workflow_mode: solo-git

project_cycle:
  current_phase: 1
  phase_name: discover
  completed_phases: []

active_flow: null
active_epic_id: null

handoff:
  next_skill: survey-context
  last_step_completed: "The remise à plat is closed: every component of the §4.1 catalogue is an identifiable module, and the rule on a frozen candidate written to while the controls measure it lives in the kernel (src/domain/candidate.ts) with a net written before the move and checked by mutation"
  open_decisions:
    - "Whether to split src/extension/index.ts (679 lines) and src/application/target.ts (702 lines) for readability; neither breaks the one-component-per-module rule, so this is comfort, not a correction"
    - "Chantier N is the next candidate and has not started; its four material questions are in its fiche, not here, and none of them is settled"
  required_reading:
    - docs/chantiers/README.md
    - docs/chantiers/N-referentiel-de-qualite-et-architecture-cible.md

git:
  branch: main
  hash: 0e1fa85e88315c1ea8234fb8018ec05a0da1fe72
