workflow_mode: solo-git

project_cycle:
  current_phase: 1
  phase_name: discover
  completed_phases: []

active_flow: null
active_epic_id: e23
active_story: e23s05

handoff:
  next_skill: develop-tdd
  last_step_completed: >
    TÂCHE 6 D'`e23s05` TENUE. Le relevé est `specs/verifications/e23s05-budgets-remesures.md`. Il
    porte la relecture des trois dossiers, l'arrêt et la relance tels que le journal les porte, les
    coûts, ce que la correction a changé, et ce qu'il n'établit pas. Il ne porte ni jeton ni chemin
    absolu. Reste la tâche 7, `npm run check`, puis la vérification de la story.

    CAMPAGNE DE RELANCE TENUE, `~/.495-campagnes/e23s05-relance-locale`, changement
    `chg_mudxhdlm52dbd65d1f`, empreinte `sha256:b7b44331…`, référence `ref_78a7b98ea7fe`. Ce qui
    était attendu a eu lieu. La spécification tombe à son quatrième appel (`4/3`), et sa fin
    `cancelled` dit « a resume runs the specify intervention again from the start ». L'arrêt est
    inscrit par `495-kernel` à la séquence 12. Rien ne se passe pendant 1 min 37 s. La relance, à la
    séquence 13, est inscrite par `jeanjerome`, `human`, `tui_session`, `session`, dans l'événement
    comme dans la colonne `actor_id` de la chaîne. L'issue est `accepted`, six `PASS`, une
    tentative, 29 appels, et quatre coûts inconnus avec leur raison. Relue contre le dossier distant,
    `compare-dossiers.ts` rend le même cas de contrat des deux côtés (sortie 0).

    TROIS FAITS NOUVEAUX, CONSIGNÉS DANS LE RELEVÉ.
    1. Sur le même modèle local, la même cible et la même demande, le débit d'ensemble vaut 4,6
       appels par minute (`e23-local`), 2,5 (`e23s05-local`) et 6,1 (relance). C'est de part et
       d'autre de la bascule à 5. Le commentaire de `policy.ts` et `D-59` décrivent la mesure à 4,6
       et restent exacts sur elle. Mais « sur le modèle local, la durée tombe la première » ne vaut
       que pour une conduite.
    2. Les appels lancés dans le même lot que celui qui dépasse la borne finissent aussi en erreur,
       même s'ils tenaient dans la borne. Le journal compte 3 appels pour l'intervention arrêtée, et
       le worker 4. Le worker finit en `failed: This operation was aborted`, et le journal inscrit
       `cancelled`.
    3. La paire `e23s05-local` contre `e23s05-distant` sort en 1 : `test/lint.test.js` n'est ajouté
       que du côté distant, alors que les deux spécifications exigent pareillement « lint ok ».

    LES TROIS DÉFAUTS DE LA CAMPAGNE SONT CORRIGÉS. Rouge commité seul (`6acd7ed`), puis une
    correction par défaut ; Preflight vert sur Node 24.21, 365 tests.
    1. La relance lève l'arrêt au nom de celui qui relance (`harness.ts`, `resume`). `change.unblock`
       admettait déjà un humain : `requireKernelAuthority` ne refuse qu'un agent, une sortie de
       modèle ou un appel d'outil, et ce refus tient toujours. Le seul appelant est `/495 resume`.
       En mode TUI, c'est le propriétaire. Sans origine humaine établie (mode `-p`, ou RPC sans
       `HARNESS495_RPC_HUMAN_ACTOR`), la commande passe `kernelUser()`, et la relance reste inscrite
       au nom du noyau : aucun propriétaire n'y est établi.
    2. Seul le producteur garde son espace de travail. La fin d'une intervention arrêtée sur la
       borne dit « the workspace keeps the unfinished work » pour `implement`, et « a resume runs
       the <rôle> intervention again from the start » pour `specify`, `prepare` et `review`. La fin
       tronquée par la durée garde sa formule pour tous les rôles. Elle est fausse de la même façon
       pour la spécification, dont l'espace est effacé à la fin de l'intervention (`clarify.ts`).
       Ce défaut-là n'est pas corrigé ici : ce que la durée fait des autres rôles relève de `D-19`.
    3. `compare-dossiers.ts` lit la fin d'une session dans le journal, pas dans ce que le worker a
       rapporté. Une nouvelle ligne, « unfinished interventions », porte le résultat inscrit et son
       détail. « schema refusals » ne compte plus qu'un rapport refusé sur une session que le
       journal dit achevée. Sur les dossiers d'`e23s05`, l'arrêt se lit désormais « cancelled:
       stopped by the tool call budget: tool call budget exceeded (4/3) … ». Sur cinq paires de
       dossiers conservés, les codes de sortie n'ont pas changé, et le rapport refusé
       d'`e23-local-rapport-refuse` reste compté comme tel.

    CAMPAGNE DE RELANCE PRÉPARÉE, SUR LE MODÈLE LOCAL, AVANT LE DÉPART. But : montrer, par une
    exécution réelle, une relance inscrite au nom du propriétaire. `dist/` est reconstruit depuis
    `e8eb02a`. Le point d'accès local répond, et la cible `~/.495-campagnes/e23s04-cible` est propre.
    Répertoire neuf : `~/.495-campagnes/e23s05-relance-locale`, dont la configuration ne porte que
    `budgets.tool_calls_per_intervention: 3`. La sortie reste celle par défaut, `omlx` sur la
    machine. La spécification locale a consommé 7 appels dans `e23s05-local`.

    CE QU'ON EN ATTEND. La spécification tombe à son quatrième appel. Sa fin, `cancelled`, dit « a
    resume runs the specify intervention again from the start » et non « the workspace keeps the
    unfinished work ». Le changement s'arrête en `clarifying` sous `budget_exhausted`, au nom du
    noyau, et rien ne démarre. Le propriétaire retire `budgets`, rouvre Pi et tape `/495 resume`.
    Le `status.changed` qui remet le changement en `ready` porte alors `jeanjerome`, `human`,
    `tui_session`, et plus `495-kernel`. La spécification est refaite, puis la suite jusqu'au
    verdict. Aucun coût n'est connu : le catalogue ne tarife pas ce modèle. La campagne n'exerce
    ni la fin d'un producteur arrêté, ni celles de la préparation et de la revue ; les tests les
    tiennent (`test/v2/tool-call-budget.test.ts`).

    LE PAS-À-PAS. Un seul pilote par changement.

      mkdir -p ~/.495-campagnes/e23s05-relance-locale
      printf '%s\n' '{ "policy": { "budgets": { "tool_calls_per_intervention": 3 } } }' \
        > ~/.495-campagnes/e23s05-relance-locale/config.json
      cd ~/.495-campagnes/e23s04-cible
      HARNESS495_DATA_DIR=~/.495-campagnes/e23s05-relance-locale pi -ne \
        -e ~/Projets/495-pi-package/dist/extension/index.js --model omlx/qwen3.8-27b-oq8e

    puis `/495 start greet doit rendre "Hello, <name>!" avec un point d'exclamation final`. À
    l'arrêt, quitter Pi. Ensuite `rm ~/.495-campagnes/e23s05-relance-locale/config.json`, rouvrir
    Pi par la même commande, et `/495 resume` jusqu'à l'issue.

    TÂCHE 5 D'`e23s05` TENUE. Les deux campagnes ont tourné sur le même `dist/` : même empreinte
    d'environnement, `sha256:54027c76…`, et même référence, `ref_78a7b98ea7fe`, `git_clean_head`.

    DISTANT, `~/.495-campagnes/e23s05-distant`, changement `chg_mudvf4f122b649e452`. Borne à 3 : la
    spécification tombe à son quatrième appel, `cancelled`, et le changement s'arrête en
    `clarifying` sous `budget_exhausted`, réessayable, avec le détail « specify intervention: tool
    call budget exceeded (4/3); the change waits for its owner, and a raised bound takes effect in
    a new session ». Rien ne démarre pendant 2 min 36 s. Borne remise à 100, nouvelle session,
    `/495 resume` : la spécification est refaite sur le même contexte, puis la préparation et
    l'implémentation, et l'issue est `accepted`, six portes `PASS`, en une tentative. 39 appels,
    16,9 par minute, 235 513 jetons, 0,1822 $ au tarif du catalogue de l'hôte, abonnement déclaré ;
    la spécification arrêtée en coûte 0,0123. La course du worker est observée : l'interruption du
    superviseur l'emporte, le worker finit `failed: This operation was aborted` avec 4 appels
    comptés, et son coût est lu malgré l'interruption. Le modèle a lancé trois lectures d'un coup ;
    la troisième dépassait, et les deux autres, comptées, ont fini en erreur une milliseconde après
    le blocage. Le dossier ne garde pas le texte de ces erreurs.

    LOCAL, `~/.495-campagnes/e23s05-local`, changement `chg_mudw3dq9d1b7514b3d`, sans configuration.
    Issue `accepted` en deux tentatives : la première a échoué au contrôle `unit` en G5. Quatre
    interventions, 31 appels, 2,5 par minute, de 1,8 à 6,2, 186 980 jetons. Les quatre coûts sont
    inconnus, avec pour raison « the host catalogue has no rate for omlx/qwen3.8-27b-oq8e », hors
    abonnement ; aucun zéro. Sur le même modèle et la même cible, `e23s04` avait mesuré 4,6 par
    minute.

    TROIS DÉFAUTS TROUVÉS PAR LA CAMPAGNE, NON CORRIGÉS PENDANT QU'ELLE TOURNAIT.
    1. La relance est inscrite au nom du noyau. `/495 resume` transmet l'identité du propriétaire,
       mais sur un changement bloqué le harnais lève le blocage sous `KERNEL_ACTOR`
       (`harness.ts:892`), et `change.unblock` n'admet que l'autorité du noyau (`decide.ts:1171`).
       Le journal dit quand le changement a repris, pas qui l'a relancé ; le §12 de la story promet
       une relance attribuée au propriétaire.
    2. La fin d'une intervention arrêtée sur la borne dit « the workspace keeps the unfinished
       work » quel que soit le rôle (`intervention.ts:229`). C'est faux pour la spécification, que
       la relance refait.
    3. `compare-dossiers.ts` range cet arrêt parmi les « schema refusals », sous « intervention
       failed » : il lit la fin rapportée par le worker, pas le `cancelled` du journal.

    TÂCHE 5 D'`e23s05` PRÉPARÉE AVANT LE DÉPART. Preflight vert sur Node 24.21, 362 tests ;
    `dist/` reproduit les sources de `499a3ba`, et les deux campagnes tournent sur lui. Pi 0.87.0,
    authentifié auprès d'`anthropic` (`pi auth check` rend `ready`). Le point d'accès local
    (127.0.0.1:8000) ne répondait pas : la campagne locale attend qu'il soit relancé.

    LA CIBLE EST CELLE D'`e23s04`, REMISE PROPRE. Un `.DS_Store` déposé par le Finder après ces
    campagnes rendait la cible `git_dirty_head` : `git status` le voit, alors que le parcours de
    l'arbre l'exclut. Il est retiré ; la référence se capture de nouveau `git_clean_head`, comme
    `ref_78a7b98ea7fe` l'était.

    LA VALEUR RETENUE, ET CE QU'ON EN ATTEND, AVANT LE DÉPART. Borne d'appels : 3 par intervention.
    Dans les deux campagnes d'`e23s04`, aucune intervention n'a consommé moins de 4 appels. La borne
    vaut pour tous les rôles, et la spécification ouvre le changement : c'est elle qui doit tomber,
    à son quatrième appel. Le noyau refuse `4/3`, le changement s'arrête sous `budget_exhausted`,
    avec un détail qui nomme le rôle, `4/3` et la relance, et aucune autre intervention ne démarre.
    Relancée avec la borne remise à 100, la spécification est refaite, puis la préparation et
    l'implémentation, jusqu'à l'issue. Le dossier dira aussi comment l'intervention arrêtée se
    termine : le worker lève sa propre erreur dans l'outil pendant que le superviseur interrompt la
    session (`worker-main.ts:142`), et `cancelled` est attendu.

    CE QUE CETTE CAMPAGNE N'EXERCE PAS. La reprise du producteur dans sa tentative. Une borne unique
    pour tous les rôles ne peut pas laisser passer la spécification et arrêter l'implémentation,
    puisque l'implémentation consomme le moins d'appels des trois. Cette reprise reste tenue par
    `test/v2/tool-call-budget.test.ts`.

    LE PAS-À-PAS. Un seul pilote par changement ; les deux campagnes l'une après l'autre. La
    déclaration de sortie et le relèvement de la borne sont des gestes du propriétaire :

      mkdir -p ~/.495-campagnes/e23s05-distant
      printf '%s\n' '{ "policy": { "egress": [ { "provider_id": "anthropic", "location": "off_machine" } ], "budgets": { "tool_calls_per_intervention": 3 } } }' \
        > ~/.495-campagnes/e23s05-distant/config.json
      cd ~/.495-campagnes/e23s04-cible
      HARNESS495_DATA_DIR=~/.495-campagnes/e23s05-distant pi -ne \
        -e ~/Projets/495-pi-package/dist/extension/index.js --model anthropic/claude-sonnet-5

    puis `/495 start greet doit rendre "Hello, <name>!" avec un point d'exclamation final`. À
    l'arrêt, quitter Pi ; le dossier est relu en lecture seule. Ensuite, retirer `budgets` de la
    configuration, rouvrir Pi par la même commande, et `/495 resume` jusqu'à l'issue. La campagne
    locale se conduit de même dans `~/.495-campagnes/e23s05-local`, sans configuration, avec
    `--model omlx/qwen3.8-27b-oq8e`, et sans arrêt attendu.

    TÂCHE 4 D'`e23s05` TENUE. Preflight vert, 362 tests. `D-59` consigne la décision : quel que soit
    le rôle, la borne d'appels arrête le changement sous `budget_exhausted` et attend le
    propriétaire. La reprise automatique est écartée, la durée garde son effet, aucune borne en
    argent n'est posée, et les valeurs par défaut ne changent pas. `D-46` porte une révision qui
    corrige la prémisse de la fenêtre d'abonnement et renvoie à `D-59`, que la capsule cite aussi.
    Chaque borne de `src/domain/policy.ts` dit, à côté d'elle, ce qu'elle arrête, laquelle tombe en
    premier aux débits mesurés, et pourquoi sa valeur ne bouge pas. La bascule est à 5 appels par
    minute, et la spécification locale, à 5,3, la franchit.

    UN CONSTAT ÉCRIT DANS `D-59`, LU DANS LE CODE SANS ÊTRE SONDÉ. La borne d'appels ne limite la
    dépense qu'intervention par intervention, car chaque continuation est une intervention nouvelle,
    avec son propre compte. Un modèle facturé qui tourne sous 5 appels par minute atteint la durée
    d'abord, et le producteur reprend alors de lui-même, jusqu'à trois fois, sans le propriétaire.
    Aucune campagne ne l'a produit.

    `dist/` a changé avec les commentaires de `policy.ts`. Les deux campagnes de la tâche 5 tournent
    donc sur le même `dist/`, reconstruit depuis ce commit ou un suivant. Suite prévue : tâche 5,
    qui demande le signal du propriétaire et consomme de l'usage facturé.

    TÂCHE 1 D'`e23s05` TENUE. Rouge commité seul, puis vert ; Preflight vert, 362 tests.
    `node scripts/measure-budgets.ts <dossier>` relit un dossier conservé en lecture seule, sans
    ouvrir Pi. Pour chaque intervention, il donne les appels d'outils, la durée, le débit, les jetons
    et le coût, puis la borne qui tomberait la première à ce débit, présentée comme une projection.
    Il donne aussi le même calcul pour l'ensemble du dossier. Un coût absent du dossier est « non
    inscrit », un coût que l'hôte n'a pas pu totaliser est « inconnu » avec sa raison, et le total
    ne devient « au moins » qu'avec une part connue : aucun zéro n'est jamais affiché. Les bornes
    viennent de `config.json` tel qu'il est au moment de la relecture, par le chargeur de
    l'extension. Le journal ne garde pas celles sous lesquelles la campagne a tourné, et la sortie le
    dit. Le test écrit ses dossiers par le vrai journal. Sur les deux dossiers d'`e23s04`, la
    relecture retrouve les chiffres du relevé : 16,0 appels par minute et 6 min 15 s en distant,
    4,6 par minute et 92 appels en local. La lecture d'un dossier est désormais partagée avec
    `compare-dossiers.ts` (`scripts/lib/dossier.ts`), dont la sortie est restée identique sur
    quatre paires de dossiers.

    UN FAIT POUR LA TÂCHE 4. La bascule est à 5 appels par minute (100 appels en 20 minutes). Le
    débit d'ensemble du modèle local est en dessous, et la durée y tombe la première. Mais sa
    spécification a tourné à 5,3 appels par minute : à ce débit, c'est le nombre d'appels qui tombe
    le premier, au bout de 18 min 42 s. « Sur le modèle local, c'est la durée » vaut donc pour le
    débit d'ensemble, pas pour chaque intervention. Le motif écrit à côté des bornes doit le dire.
    Suite prévue : tâche 4.

    TÂCHE 2 D'`e23s05` TENUE, sur la branche `borne-d-appels-et-cout`. Rouge commité seul, puis
    vert ; Preflight vert, 354 tests. Chaque `intervention.finished` porte un `cost` : le total que
    l'hôte calcule pour la session (`getSessionStats()`), lu par le worker avant de fermer la
    session, avec sa base (le catalogue de l'hôte) et l'emploi d'un abonnement
    (`isUsingSubscription()`). Le coût est inconnu, avec sa raison, dans trois cas : le catalogue ne
    porte aucun tarif pour le modèle, la session ne rapporte aucun usage, ou la fin a été synthétisée
    sans le worker (silence, pause, reprise, annulation). Un arrêt sur la borne d'appels porte ce qui
    a été dépensé avant lui. Le test oppose la lecture à de vraies sessions de l'hôte sur un point
    d'accès local, puis au vrai worker ; le cas d'abonnement ne sort pas de la machine, vérifié sous
    Seatbelt, réseau sortant interdit. Le champ est absent des dossiers écrits avant : la relecture
    de la tâche 1 doit le dire au lieu d'afficher zéro. Suite prévue : tâche 1, puis 4.

    LE PLAN SE TROMPAIT SUR LES CONTRATS. Aucun contrat publié ne décrit l'intervention, et
    `npm run contracts` ne change aucun des 28 schémas. Ce qui change de forme, c'est l'événement du
    journal et le protocole du worker, et, comme pour tout changement de code, l'empreinte du
    harnais. La tâche est corrigée en ce sens.

    TÂCHE 3 D'`e23s05` TENUE, avant la 2. Rouge commité seul, puis vert ; Preflight vert, 344
    tests. Quel que soit le rôle, une intervention qui dépasse sa borne d'appels arrête le
    changement sous `budget_exhausted`, que la relance lève. C'est le refus du noyau, lu côté
    application (`intervention.ts`), qui décide, et non l'événement final du worker ; la course du
    worker réel est donc tranchée par construction, mais elle n'est pas encore observée.
    L'intervention reste inscrite `cancelled`, et son détail nomme la borne. La note de reprise du
    producteur compte désormais aussi les arrêts sur borne et les pauses.

    `e23s05` est planifiée : la fiche est écrite au format de story, et le journal de tâches en
    compte sept, chacune avec une commande qui échouait au moment du plan. Preflight était vert
    avant le plan (338 tests).

    LA PRÉMISSE DE LA STORY EST TOMBÉE. L'epic disait qu'avec un modèle frontière, la borne cessait
    d'être le temps pour devenir la fenêtre d'abonnement. Pi dit le contraire (`providers.md:36`) :
    l'usage par un harnais tiers est facturé au jeton, sur l'usage supplémentaire, hors des
    plafonds du plan. La capsule est corrigée ; `D-46` le sera par la décision que la tâche 4
    consigne.

    UN DÉFAUT SONDÉ. Avec un producteur scripté, le noyau et l'application étant réels : la borne
    de durée suspend l'intervention (`truncated`, reprise sur l'espace de travail) ; la borne
    d'appels l'annule (`cancelled`), et le changement est bloqué comme `execution_error`, avec pour
    détail « producer intervention cancelled ». Lu dans le code sans être sondé : la spécification
    lève une erreur de configuration réessayable, la préparation continue sur un résultat partiel,
    et la revue est inscrite invalide. Le motif `budget_exhausted` est déclaré dans
    `contracts/v1/common.ts` et rien ne l'émet. Sur le modèle distant, c'est la borne d'appels qui
    tombe la première. Non sondé sur le worker réel : son propre contrôle
    (`worker-main.ts:137`) lève une erreur dans l'outil pendant que le superviseur interrompt la
    session ; la campagne de la tâche 5 dira lequel des deux l'emporte.

    UNE DÉCISION DU PROPRIÉTAIRE, 2026-09-23. Quand le nombre d'appels est atteint, le changement
    s'arrête et attend sa relance, sous `budget_exhausted`, et rien n'est dépensé au-delà sans son
    geste. L'autre option, que la reprise se fasse d'elle-même comme pour la durée, est écartée.
    La durée garde son effet (`D-19`).

    CE QUE L'HÔTE REPORTE DU COÛT. `getSessionStats()` totalise l'usage et le coût sur toutes les
    entrées de la session, résumés et rafraîchissements du cache compris.
    `modelRuntime.isUsingSubscription()` dit si le fournisseur est employé par abonnement. Le coût
    est calculé au tarif du catalogue (`calculateCost`, pi-ai `models.js`), et un tarif absent vaut
    zéro (`models.md`). Le modèle local déclare 0 partout ; son coût sera inscrit inconnu, jamais
    nul (`NFR-06`, `AGT-07`).

    HORS DE LA STORY, ET ÉCRIT COMME TEL. Aucune borne en argent. Les valeurs par défaut ne
    changent pas. La préparation relancée refait son intervention, parce qu'elle ne garde pas son
    espace de travail.

    RESTE À L'EPIC, après `e23s05` : `e23s06`, qui observe ce que le fournisseur écrit au-dessus des
    instructions locales.

    DÉJÀ TRANCHÉ, À NE PAS ROUVRIR SANS UN FAIT NOUVEAU. Les deux campagnes d'`e23s04` n'ont pas
    tourné sur le même `dist/`. La campagne distante n'est pas reconduite, et la réserve reste
    écrite au relevé, à la matrice et au §19 de la story.

    TROIS DETTES CONNUES, INCHANGÉES. L'identité humaine déduite de `node:os` ne dit pas ce qui a
    été cherché chez Pi. L'événement de reprise déclaré n'est émis par rien, et attend l'epic qui
    touche ce contrat. La table du bloc fournisseur reste une attente que rien ne confronte jusqu'à
    `e23s06`.
