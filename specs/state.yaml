workflow_mode: solo-git

project_cycle:
  current_phase: 1
  phase_name: discover
  completed_phases: []

active_flow: build_epic
active_epic_id: e01
active_story: e01s02

epic_cycle:
  step: 6
  completed_steps: "0,1,2,3,4,5"
  story_bcps: 5

handoff:
  next_skill: release-branch
  last_step_completed: >
    Tour 5 de `request-review` de e01s02 le 2026-09-26, sur `git diff e85b112 9105a57` : les deux
    relecteurs passent, sans point à corriger ; la revue est close à `9105a57`. Leurs mutations
    (règle réduite à `stop_retryable`, actions inversées, vides ou en trop, autre code, reprise qui
    ignore la règle) échouent toutes au test du refus de la pause. Laissé : `e25s02-cycle-rouge-vert.md:68`
    dit que la reprise ne lève que les blocages réessayables et tait `execution_error`, ligne
    antérieure à la branche. Enregistrements ancrés à `9105a57` (CONVENTIONS.md § Review, règle 5) :
    `e01s02-verify.yaml`, `NFR-e01s02.json`, les commandes des tâches du journal rouge-vert, et la
    section e01s02 de `specs/security/REVIEW.md`, relue sur `git diff 31e81ba 9105a57 -- src/`.
    `npm run build && npm run check` sous Node 24.21.0, 16:22:14Z à 16:24:46Z : sortie 0, 490 tests,
    `dist/` inchangé. Le jugement des rapports n'a changé que dans ses commentaires : les rapports
    réels #100 et #187 ne sont pas jugés de nouveau. La mise en route et les sept campagnes restent
    celles de `31e81ba`, ce que l'enregistrement déclare. Prochaine étape : `release-branch`.

    `respond-review` a traité le tour 4 de `request-review` de e01s02 le 2026-09-26, sur
    `2c33b93..e85b112`. B passe, A échoue sur un point à corriger : le relevé et ce suivi disaient
    « non levable » l'erreur d'exécution qu'une reprise lève ; le mot est « non réessayable », celui
    de D-38 pour `stop_retryable`. Retenu aussi, relevé par A et B : le refus de la pause est comparé
    par un objet passé à `assert.throws`, qui affiche les actions attendues et reçues quand il échoue,
    au lieu d'un prédicat sur `JSON.stringify`. Sous la mutation qui ne garde que `stop_retryable`
    dans `resumeLiftsStop`, le test échoue encore (exit 1) et nomme `resume` manquant. Preflight
    verte sous Node 26.9.0, 16:11:40Z à 16:14:59Z, 490 tests. Prochaine
    étape : tour 5 de `request-review`, le dernier, sur `git diff e85b112 HEAD`.

    `respond-review` a traité le tour 3 de `request-review` de e01s02 le 2026-09-26, sur
    `aefa6b7..9a0ebe5`. A passe, B échoue sur un point à corriger : la branche `execution_error` de
    `resumeLiftsStop` n'était pas testée, et la retirer passait toute la suite. `54ce26f` fixe qu'un
    changement arrêté par une erreur d'exécution non réessayable refuse la pause en nommant `resume` et
    `cancel`, et que la reprise lève l'arrêt ; le test échoue sous cette mutation. Le message de
    `9a0ebe5`, qui nommait le tour de revue, est reformulé en `2c33b93` (même arbre). Consider
    laissés : la fin d'intervention au coût inconnu écrite en quatre endroits (`block`,
    `changeCancel`, `harness.pause`, `harness.resume`) ; une reprise que `changeUnblock` refuse sur un
    effet incertain, que le refus de la pause nommerait sans qu'elle agisse, non atteignable. Preflight
    verte sous Node 26.9.0, 15:25:18Z à 15:28:37Z, 490 tests. Prochaine étape : tour 4 de
    `request-review` sur `git diff 2c33b93 HEAD`.

    `respond-review` a traité le tour 2 de `request-review` de e01s02 le 2026-09-26, sur
    `f2a52cd..aefa6b7`. Le point à corriger de B : la garde de `changePause` se contournait sur un
    changement bloqué pendant que son intervention tournait, car `/495 pause` la terminait et
    `intervention.finished` rendait le changement `ready`. BUG-2026-09-26T142500, notée corrigée à
    tort, est rouverte ; la même cause faisait lever par `/495 resume` un arrêt qu'aucune reprise ne
    lève (BUG-2026-09-26T145000, sur `main`). Correctif à la source : `block` termine l'intervention
    en cours dans la décision de l'arrêt (rouge `287123c`, vert `d6b0bd5`). Retenus aussi : `paused`
    seulement sur le conflit de révision de la pause (`9dd736a`, `4437873`), et le refus de la pause
    nomme `resume` quand une reprise lève l'arrêt et `cancel` toujours, par la règle `resumeLiftsStop`
    que lit aussi `harness.resume` (`03f9878`, `751c2d1`). Isolation par arbre détaché ; Preflight
    verte sous Node 26.9.0, 14:58:41Z à 15:02:10Z, 490 tests. Laissés : un journal déjà écrit avec un
    changement bloqué et une intervention `running` reste exposé ; la vue (`views.ts`) ne dit « resume
    retries it » que pour un arrêt levable et tait `execution_error` ; une pause refusée pour un effet
    en vol inscrit encore la fin de l'intervention (sur `main`). Prochaine étape : tour 3 de
    `request-review` sur `git diff aefa6b7 HEAD`.

    `fix-bug` a corrigé BUG-2026-09-26T142500 le 2026-09-26 : un changement bloqué refuse la pause,
    qui effaçait son arrêt (motif, détail, levée par une reprise). Rouge `0d67f46` (2 échecs sur 59),
    vert `6d5a3e3`, isolation par arbre détaché ; Preflight verte sous Node 26.9.0, 14:30:43Z à
    14:34:00Z, 486 tests. Le correctif atterrit avec la branche, en commit séparé. Prochaine étape :
    tour 2 de `request-review` sur `git diff f2a52cd HEAD`, qui relit aussi le correctif de la pause
    dans la boucle d'avance du harnais (`bc55ce2`), dont l'effet dépasse la clarification.

    `respond-review` a traité le premier tour de `request-review` de e01s02 le 2026-09-26, sur
    `main...f2a52cd`. Le point à corriger de B (la condition `blocked &&` de `specificationHistory`
    sans test) cachait un défaut que la branche rend atteignable : une pause pendant une réécriture,
    faite comme `/495 pause`, était écrasée par un blocage `execution_error`, et la reprise, en le
    levant, relançait la borne (6 interventions au lieu de 5). Rouge `bcdeb0b`, vert `bc55ce2` : le
    pas qui échoue sous une pause rend `paused`. L'effacement d'une liaison héritée par une
    déclaration propre qui ne tient pas est fixé par `cddf2c8` (tue la mutation), et les
    commentaires de `state.ts` nomment le dernier acte humain (`098f66d`). Preflight verte sous Node
    26.9.0, 14:14:56Z à 14:18:24Z, 484 tests. Laissés : le test de secours de `clarify.ts:33`, la
    reprise sans humain déclaré (déjà notée dans `specs/security/REVIEW.md`), le verdict unique.
    Prochaine étape : `fix-bug` pour la pause d'un changement déjà arrêté, qui efface son arrêt
    (défaut antérieur à la branche), puis le tour 2 de `request-review` sur `git diff f2a52cd HEAD`.

    Recette de e01s02 confirmée par le propriétaire le 2026-09-26 à 13:37:14Z, sur les sept
    campagnes et les limites déclarées ; e01s02 passe à `done` dans `execution-status.yaml`.
    Prochaine étape : audit-code sur `git diff main...HEAD`.

    verify-work de e01s02 conduit le 2026-09-26 à `31e81ba`, en attente de la confirmation du
    propriétaire. Preflight verte sous Node 24.21.0, 13:29:50Z à 13:32:22Z, 482 tests. Sept campagnes
    dans un vrai `pi --mode rpc` sur des copies de `java-flashnext-L2` : l'arrêt avant G0
    (`e01s02-arret`) et son contrôle négatif sur `38bf5a3` (mandat de la campagne perdue, G1 FAIL) ;
    la reprise avec le modèle local (`e01s02-reprise`, une intervention, Q1 déclarée, G0 et G1 PASS)
    et son contrôle négatif (même refus rejoué) ; l'abandon ; le refus de G1 qui nomme `cancel` et son
    contrôle négatif. Rapports réels #100 et #187 de `java-flashnext-L` jugés pour la tâche 3.
    Fictions déclarées : le dossier coupé après l'événement 183, et la réécriture qui perd encore Q1
    simulée par l'agent scripté. Relevés : `e01s02-verify.yaml`, `NFR-e01s02.json`, section e01s02
    de `specs/security/REVIEW.md`. Après confirmation : `execution-status.yaml`, puis audit-code.

    Tâche 5 de e01s02 passée le 2026-09-26 : `npm run build && npm run check` sous Node 26.9.0 à
    `66b6941`, 13:03:18Z à 13:06:35Z, sortie 0, 482 tests ; les cinq tâches et la story sont en
    `passing`. Prochaine étape : verify-work, avec une campagne réelle dans Pi et son contrôle
    négatif sur `38bf5a3`.

    Tâche 4 de e01s02 passée le 2026-09-26 : rouge `e5f0f28` (test seul, 1 échec sur 15), vert
    `b34547a` ; isolation contrôlée par arbre détaché, Preflight verte sous Node 26.9.0, 482 tests.
    Un refus de G1 nomme `cancel` dans la décision de gate et dans le détail de l'arrêt ;
    `revise_requirements` n'apparaît plus dans `src/`. Prochaine étape : tâche 5 (`npm run check`,
    déjà vert à `b34547a`), puis verify-work.

    Tâche 3 de e01s02 passée le 2026-09-26 : rouge `292b904` (test seul, 3 échecs sur 85), vert
    `931a7fb` ; isolation contrôlée par arbre détaché, Preflight verte sous Node 26.9.0, 481 tests.
    La déclaration propre d'un rapport ne porte une réponse que si elle tient dans ses exigences,
    comme ses déclarations héritées et comme G1 en juge. Le test `R1-trimee` de `harness.test.ts`
    s'arrête désormais avant G0 et la reprise le mène à sa clôture ; les motifs de refus de G1 qu'il
    fixait sont repris par un test de règle dans `change-rules.test.ts`. Prochaine étape :
    develop-tdd, tâche 4 (un refus de G1 nomme `cancel` au lieu de `revise_requirements`).

    Tâche 2 de e01s02 passée le 2026-09-26 : rouge `1fc82ff` (test seul, 2 échecs sur 12), vert
    `4f17bd3`, refactorisation `6180916` (`sinceLastAnswer` devient `sinceLastHumanAct`) ; isolation
    contrôlée par arbre détaché, Preflight verte sous Node 26.9.0, 478 tests. La borne de progression
    se coupe à la dernière réponse matérielle ou à la dernière levée d'un arrêt : la reprise obtient
    une réécriture, dont la demande dit la réponse à déclarer, puis la borne s'applique. L'abandon
    tenait déjà ; `0afe199` fixe qu'un agent, une sortie de modèle ou un appel d'outil ne lève pas
    l'arrêt (tué par la mutation qui retire `requireKernelAuthority` de `changeUnblock`). Prochaine
    étape : develop-tdd, tâche 3 (une déclaration ne porte une réponse que si elle tient dans les
    exigences du rapport).

    Tâche 1 de e01s02 passée le 2026-09-26 : rouge `103ce48` (test seul, 7 échecs sur 40), vert
    `8852a2b` ; isolation contrôlée par arbre détaché, Preflight verte sous Node 26.9.0, 474 tests.
    Un rapport qui perd une réponse et que la borne interdit de rouvrir arrête le changement en
    clarification (stagnation levable, détail qui nomme la réponse, `resume` et `cancel`) ; aucun
    mandat n'est proposé. Un septième test change d'attendu, que le plan ne nommait pas :
    `harness.test.ts`, la spécification qui ignore toujours une réponse (RM-011). Journal :
    `specs/verifications/e01s02-cycle-rouge-vert.md`. Prochaine étape : develop-tdd, tâche 2 (la
    reprise réécrit la spécification). À la lecture du code, non exécuté : une reprise lève l'arrêt,
    et la borne, qui ne voit pas la reprise, arrête de nouveau le changement sans intervention.

    Kickoff fait le 2026-09-26 : spécification de e01s02 versée sur `main` (`38bf5a3`), branche
    `specification-arretee-avant-g0` créée depuis ce point, `main` à jour avec `origin/main` avant
    ce commit. Preflight verte sous Node 26.9.0, 12:01:37Z à 12:04:50Z, 474 tests, `dist/`
    reconstruit à l'identique. Aucun verrou de story : `specs/agent-locks.yaml` n'existe pas.
    Prochaine étape : develop-tdd sur `test/v2/specification-reopening.test.ts`, tâche 1.

    Étape 2 faite le 2026-09-26 : spécification de e01s02 écrite
    (`e01s02-une-specification-qui-ne-progresse-plus-arrete-le-changement-avec-un-recours.md`,
    `e01s02-tasks.yaml`, cinq tâches en `failing`), format tenu par `lint:story-format`. Relevé du
    code qui fixe la forme du recours : après G0 les phases ne reviennent jamais en arrière, donc la
    réécriture n'est possible qu'en clarification ; `/495 resume` lève déjà un arrêt levable et
    `/495 cancel` clôt comme abandonné ; le motif d'arrêt `stagnation` et le code `STAGNATION`
    existent sans servir à la spécification ; seul `resume` émet `change.unblock`, refusé à un
    agent, une sortie de modèle et un appel d'outil. D'où : l'arrêt est une stagnation levable en
    clarification, la borne de progression repart de la dernière réponse ou de la dernière levée
    d'un arrêt, et un refus de G1 nomme `cancel`. Deux points ajoutés à la capsule, écrits dans la
    story : une déclaration propre au rapport qui nomme une exigence absente ou aucune obligatoire
    ne porte plus la réponse (le prédicat de G1, qui menait sinon au même refus sans issue) ; les
    défauts de structure que G1 refuse ne sont pas jugés avant G0, seul le nom de leur issue
    change (§18). Rayon d'impact : `specificationStanding` n'a qu'un appelant (`clarify`),
    `specificationHistory` en a deux (`clarify`, et `priorDiagnostics` par `specify`, dont la
    somme des deux moitiés ne change pas) ; aucun contrat, événement ni composant nouveau. Les
    tests 6b, 6c, 6g, 6h, 6j et le report d'une déclaration de `harness.test.ts` changent d'attendu.
    Prochaine étape : kickoff-branch, puis develop-tdd sur `test/v2/specification-reopening.test.ts`.

    Étape 1 faite le 2026-09-26 : e01s02 confirmée, démarrée à 2026-09-26T13:51:00+02:00. L'étape 0
    tient déjà : le modèle de menace de e01 couvre e01s02 par M2 (le refus de G1 reste fermé ; le
    recours relance ou arrête, il n'adopte jamais un document qui perd une réponse) et M7 (chaque
    reprise relance une intervention, déclenchée par un humain). Point de départ vérifié : `main` à
    `9b87f95`, propre ; aucun fichier de `src/`, `test/` ou `scripts/` n'a changé depuis `07f4c38`,
    dernière Preflight verte (Node 24.21.0, 474 tests), que kickoff-branch rejouera. Un seul défaut
    ouvert, `BUG-2026-09-23T155707` (budget de tentatives), sans effet sur e01s02 sinon qu'il ne
    borne pas les reprises (M7). Le défaut que la story corrige est présent :
    `specificationStanding` rend `settled` dès que `reopen` est faux, même quand `ignored` n'est pas
    vide (`src/domain/change/state.ts:446`), et `clarify` porte alors ce rapport jusqu'au mandat
    (`src/application/phases/clarify.ts:33`). G1 le refuse avec `revise_requirements`
    (`src/domain/change/decide.ts:464`, `src/application/phases/specify.ts:68`), action qu'aucune
    commande ne tient. Prochaine étape : plan-work, spécification de e01s02.

    e01s01 versée sur `main` le 2026-09-26 par avance rapide, sur décision du propriétaire.
    Preflight verte sur `main` sous Node 24.21.0, 11:41:05Z à 11:43:34Z, 474 tests. Couverture
    mesurée dans un arbre détaché : 96,64 % de lignes (94,99 % sur `main` avant), 100 % pour
    `state.ts`, `artifacts.ts` et `clarify.ts`. Traçabilité : `lint:traceability` vert, matrice
    générique non tenue (même dérogation que les stories précédentes). Epic e01 en cours : e01s02 à
    e01s05 restent à faire, la capsule n'est pas archivée. `main` n'est pas poussée.

    Enregistrements ancrés le 2026-09-26 à `07f4c38` (CONVENTIONS.md § Review, règle 5) :
    `e01s01-verify.yaml`, `NFR-e01s01.json`, les commandes des tâches du journal rouge-vert, et la
    section e01s01 de `specs/security/REVIEW.md`, relue sur `git diff 8a7c8dd 07f4c38 -- src/`.
    `npm run build && npm run check` sous Node 24.21.0 : sortie 0, 474 tests, `dist/` inchangé. Le
    rapport 183 de `java-flashnext-L2`, jugé à `07f4c38` sur une copie coupée du dossier, donne
    toujours ignored [Q1], reopen true. La mise en route et les deux campagnes restent celles de
    `8a7c8dd`, ce que l'enregistrement déclare. L'auto-revue (`AUDIT-e01-e01s01.md`) reste datée de
    `4892a6d` : elle n'a pas été refaite. Prochaine étape : `release-branch`.

    Troisième tour de `request-review` le 2026-09-26, sur `git diff 2b6206d HEAD` : les deux
    relecteurs passent, sans must-fix ni should-fix ; le gate est franchi à `62f96b5`. Sur décision
    du propriétaire, les deux trous de test relevés en consider sont comblés (`6339f36`). 6i : le
    rapport sur lequel la dernière réponse a été donnée est réécrit dès qu'il en ignore une, même
    quand ce n'est pas le premier. 6j : ce qu'il hérite d'un rapport plus ancien compte comme déjà
    porté. Les deux tests fixent un comportement qui tenait ; chacun échoue sous la mutation qu'il
    vise, et ces trois mutations passaient la suite d'avant. Consider laissés ouverts : filtre
    `material.has` de `specificationHistory` non testable faute de réponse non matérielle, rapport
    illisible qui décale la coupure d'un rapport (tolérance antérieure), `priorDiagnostics` qui lit
    le journal pour recoller les deux moitiés, type de retour répétant `SpecificationHistory`.
    Preflight verte à `6339f36` sous Node 26.9.0, 474 tests. Prochaine étape : ancrer les
    enregistrements de `specs/verifications/` et `specs/security/REVIEW.md` (CONVENTIONS.md
    § Review, règle 5), puis `release-branch`.

    `respond-review` a traité le deuxième tour de `request-review` le 2026-09-26. Must-fix (A) : la
    borne repartait à chaque entrée dans `clarify`, si bien qu'une adoption humaine du mandat
    rouvrait sans fin le rapport adopté, et qu'un refus de G0 payait une intervention par tour. Rouge
    `0a24b24` (6g), vert `9cc312a` : la progression se mesure depuis la dernière réponse matérielle,
    dont l'ordre est lu dans le journal du changement (`specificationHistory`), et l'historique est
    passé partagé en deux plutôt qu'avec un compte. 6h fixe la règle « celui-là compris » (mutation
    vérifiée). Should-fix (B) : la borne de §14 et du commentaire était fausse d'un ; elle vaut une de
    plus que les réponses enregistrées. Spec alignée (§1, §5, §8, §14, scénarios 6g et 6h).
    Preflight verte à `9cc312a` sous Node 26.9.0, 472 tests. Prochaine étape : troisième tour de
    `request-review`, sur `git diff 2b6206d HEAD`.

    `respond-review` a traité le premier tour de `request-review` le 2026-09-25. Les deux relecteurs
    avaient relevé le même should-fix : la progression se mesurait contre tous les rapports du
    changement, si bien qu'une réponse donnée après un rapport qui n'avait rien gagné n'atteignait
    aucune réécriture, et le changement finissait `blocked` à G1. Rouge `5720678` (test 6f seul),
    vert `5364797` : la progression se mesure contre les rapports écrits depuis la reprise de la
    clarification, et le rapport trouvé à la reprise est rouvert dès qu'il perd une réponse. Spec
    alignée (§1, §5, §8, §14, scénario 6f). Preflight verte à `5364797` sous Node 26.9.0, 470
    tests. Remarques consider laissées ouvertes : tests de 6d et d'une intervention bloquée au
    milieu de la boucle, reprise après interruption entre l'écriture du rapport et l'ouverture de sa
    question, `SpecificationReportView` de même forme que `DeclaringReport`. Prochaine étape :
    deuxième tour de `request-review`, sur `git diff a1bffc5 HEAD`.

    `audit-code` fait le 2026-09-25 pour e01s01 : PASS après correction (`4892a6d`). Le champ
    `questions` de `SpecificationReportView`, que la règle de réouverture ne lit plus, est retiré.
    Le `rounds` copié d'une suite à l'autre devient `specificationRounds` dans
    `test/helpers/harness-fixture.ts`. Preflight verte à `4892a6d` sous Node 26.9.0, 469 tests.
    Rapport : `specs/verifications/AUDIT-e01-e01s01.md`. Prochaine étape : `request-review`.

    Étape 5 faite le 2026-09-25 : `verify-work` a vérifié e01s01, et le propriétaire a confirmé la
    recette. Le changement perdu de `java-flashnext-L2` est repris dans un vrai `pi --mode rpc`, avec
    `omlx/qwen3.8-27b-oq8e` et le bac à sable réel, sur une copie du dossier coupée juste après le
    rapport qui perdait la liaison de Q1 (fiction déclarée). Sur la branche (`e01s01-reprise`), la
    spécification est rouverte une fois, et sa demande marque Q1 « à déclarer ». Le rapport lie Q1 à
    trois exigences obligatoires, G0 et G1 passent, et le document adopté porte Q1 à Q7. Sur
    `bdebd23` (`e01s01-reprise-negatif`), rien n'est relancé : le mandat est identique à celui du
    2026-09-19, et G1 refuse Q1. 6b, 6c et 6e reposent sur leurs tests. Preflight verte sous Node
    24.21.0 à `8a7c8dd` (469 tests), aucun constat de sécurité à confiance 8 ou plus. Preuves :
    `specs/verifications/e01s01-verify.yaml`, `NFR-e01s01.json`, section e01s01 de
    `specs/security/REVIEW.md`. Prochaine étape : `audit-code`.

    develop-tdd fait le 2026-09-25 sur `rapport-rouvre-la-specification` : rouge `60c2c34` (test
    seul, 3 échecs sur 4 en isolation, et le test de `harness.test.ts` à 3 interventions au lieu
    de 4), vert `ada5ada`. La réouverture retient toute réponse matérielle que le rapport ne porte
    pas, `clarify` juge le rapport qu'il vient d'obtenir, et la borne se mesure contre tous les
    rapports antérieurs. Les trois tâches de `e01s01-tasks.yaml` sont `passing`. Preflight verte
    sous Node 24.21.0, 469 tests. Journal : `specs/verifications/e01s01-cycle-rouge-vert.md`.
    Aucun script de vérification manuelle dans la capsule ; la campagne réelle relève d'e01s05.
    Prochaine étape : verify-work.

    Kickoff fait le 2026-09-25 : planification de e01 versée sur `main` (`bdebd23`), branche
    `rapport-rouvre-la-specification` créée depuis ce point. Preflight verte sous Node 24.21.0,
    465 tests, `dist/` reconstruit à l'identique. Prochaine étape : develop-tdd sur
    `test/v2/specification-reopening.test.ts`.

    Étape 2 faite le 2026-09-25 : spécification de e01s01 écrite
    (`e01s01-un-rapport-qui-defait-une-liaison-sans-rien-demander-rouvre-la-specification.md`,
    `e01s01-tasks.yaml`, trois tâches en `failing`), format tenu par `lint:story-format`. Le relevé
    de `java-flashnext-L2` a corrigé la capsule sur deux points, reportés dans sa position de
    e01s01. D'abord, le rapport 183 qui perd Q1 est issu d'une réouverture et ne pose aucune
    question, et `clarify` ne juge jamais le rapport qu'elle vient d'obtenir. Aligner le prédicat
    seul n'aurait donc rien rouvert. Ensuite, une fois ce jugement ajouté, la borne comparée au seul
    rapport précédent laisse un modèle qui oscille relancer sans fin. Elle se mesure donc contre
    tous les rapports antérieurs. Le test existant « stops carrying a declaration… » de
    `test/v2/harness.test.ts` change d'attendu : une réouverture de plus avant le refus de G1.
    Prochaine étape : kickoff-branch, puis develop-tdd sur
    `test/v2/specification-reopening.test.ts`.

    Étape 1 faite le 2026-09-25 : e01s01 confirmée comme story de départ, démarrée à 2026-09-25T17:07:59+02:00.
    Point de départ vérifié : `main` à `02e86ef`, propre hors des fichiers de planification de e01 ;
    aucun fichier de `src/`, `test/` ou `scripts/` n'a changé depuis `68cf582`, dernière Preflight
    verte (Node 24.21.0, 465 tests), que kickoff-branch rejouera. Un seul défaut ouvert,
    `BUG-2026-09-23T155707` (budget de tentatives), sans effet sur e01s01. Le défaut que la story
    corrige est présent : `answersTheReportIgnores` filtre encore sur `asked.has(q.id)`
    (`src/domain/change/state.ts:342`).

    Étape 0 faite le 2026-09-25 : modèle de menace de e01 écrit
    (`specs/security/epics/e01/THREAT_MODEL.md`), risque moyen, sept menaces. Une reste sans story :
    M1, le modèle qui déclare une réponse humaine « non observable » la soustrait à toute obligation
    à G1 (`decide.ts:426`). Le propriétaire l'a fait entrer dans e01s03 le même jour (5 BCP, epic à
    23) : la déclaration du rapport devient une proposition que l'humain confirme.

    e01 ENGAGÉ le 2026-09-25. Capsule écrite (`specs/epics/e01-reponse-humaine-atteint-les-exigences/epic.yaml`),
    cinq stories, 23 BCP, aucune spécification encore : elles s'écrivent une à une (D-54). Le
    propriétaire y a fait entrer le même jour la révocation d'une réponse donnée par erreur et la
    clôture d'une question qui n'est plus matérielle (e01s03, e01s04). L'état à
    l'ouverture est établi dans le code : D-37 et D-39 tiennent déjà la réponse jusqu'au document de
    G1 ; restent l'écart entre le prédicat de réouverture et celui de G1 (`answersTheReportIgnores`
    ne retient que les questions que le rapport a posées), l'impasse quand la borne de progression
    refuse de relancer, une révocation que le noyau enregistre sans la rendre opérante pour une
    réponse, et l'absence de toute campagne réelle au-delà de G1. Prochaine étape :
    modèle de menace de l'epic, puis spécification de e01s01.

    Chantier de communication `e26` activé le 2026-09-25 par le propriétaire, à la publication de la
    0.2.1 (J1). Les cinq messages de l'exploration sont rédigés dans
    `specs/epics/e26-pi-495-est-essaye-par-ceux-qu-il-sert/campagnes/exploration.md` ; le premier se
    publie à J4, le 2026-09-28, et s'inscrit dans `growth.csv` avant de partir. Il ne change rien à
    l'ordre écrit : le suivant reste `e01`.

    `e25` EST TENU, et aucun epic n'est engagé. Ses cinq stories sont versées sur `main`, poussé sur
    `origin/main` jusqu'à `88c6422`. Le modèle choisi dans Pi est admis sans configuration (`D-61`),
    lu à chaque intervention, situé par son adresse et annoncé quand il est hors de la machine ;
    `config.json` est validé par un schéma strict. Recette tenue par trois campagnes réelles, dont une
    par l'abonnement Anthropic (`specs/verifications/e25s04-modele-choisi-admis.md`). Preflight verte
    sous Node 24.21.0, 455 tests. Défaut encore ouvert : `BUG-2026-09-23T155707`, le budget de
    tentatives écrit dans `config.json` n'atteint jamais un changement. Le suivant dans l'ordre écrit
    est `e01`. Les branches versées de l'epic sont supprimées.

    pi-495 0.2.0 publiée sur npm le 2026-09-24 par le propriétaire (2FA), depuis `3d30ffe` : empreinte
    `d53c9d44d0edd027f28ee343a8c45d9ff42804a8` identique à la simulation, 345 fichiers ; étiquette
    `v0.2.0` créée et poussée avec `main`.

    pi-495 0.2.1 publiée sur npm le 2026-09-25 par le propriétaire (2FA), depuis `68cf582` : empreinte
    `267d922bf957e6f8299b12508ac38d658377d591` identique à la simulation, 345 fichiers ; étiquette
    `v0.2.1` et release GitHub créées et poussées. Elle ajoute la consommation dans la barre du bas, la
    section des exigences au rapport et la démonstration du README (reprise en `pi.video`), et corrige
    la langue de la première intervention, les messages affichés deux fois dans l'interface texte et
    la traduction de l'écran de revue. Preflight verte sous Node 24.21.0 à `68cf582` (465 tests).

    e25s04 versée sur `main` le 2026-09-24 par avance rapide, sur décision du propriétaire ; l'epic
    e25 est terminée. Preflight verte sous Node 24.21.0 à `535ea23` (455 tests).

    Étape 6 rouverte puis close le 2026-09-24 pour e25s04. Après le tour 3, le propriétaire a décidé
    de corriger ici `BUG-2026-09-24T175500` : dans l'interface texte, un diagnostic en file n'est
    plus notifié une seconde fois à l'écran quand le premier `/495` le remet. Rouge `447df46`, vert
    `3a9ef82`, troisième cas `4591fe8` (rouges tous trois sur le code de `a888f48`). Observé dans la
    vraie interface texte par pseudo-terminal (`~/.495-campagnes/e25s04-tui`) : avertissement puis
    information avant, avertissement seul après. Tours 4 et 5 : A et B trouvent le correctif juste ;
    l'audit et la revue de sécurité le couvrent, le banc est rejoué avec les commandes qu'il nomme
    (`535ea23`). A et B passent le tour 5, plafond atteint sans point ouvert. Relevés ancrés à
    `535ea23`, Preflight verte sous Node 24.21.0, 18:21:59Z à 18:24:21Z, 455 tests. Prochaine étape :
    verser sur `main`, sur décision du propriétaire.

    Étape 6 close le 2026-09-24 pour e25s04 : `audit-code` passe (`AUDIT-e25-e25s04.md`), la
    relecture A et B passe au tour 3 sur `a888f48`. Tour 1 : `D-61` disait que la ligne de coût nomme
    le modèle joint (à corriger, les deux), les dossiers portent le chemin personnel que la revue de
    sécurité disait absent, `D-46` et `D-57` promettaient encore une destination jugée, une ligne
    AGT-07 ajoutée à la matrice archivée `[P0]` ; corrigé. La spécification (§12, §14, §17) dit
    désormais que la preuve du fournisseur joint est la strate observée dans la requête — corrigée
    après la confirmation du propriétaire, à lui dire. Défaut trouvé, consigné ouvert :
    `BUG-2026-09-24T175500`, l'interface texte dit deux fois à l'écran l'annonce d'un modèle distant
    et chaque diagnostic d'ouverture. La story ne se clôt pas avant que le propriétaire l'ait tranché
    (spec 6c). Preflight verte sous Node 24.21.0 à `a888f48`, 17:57:13Z à 17:59:33Z, 452 tests.

    Étape 5 faite le 2026-09-24 : recette confirmée par le propriétaire.

    Étape 4 faite le 2026-09-24 pour e25s04, sur la branche `modele-choisi-admis` : les quatre tâches
    passent. `D-61` écrite, `D-53` porte qu'elle est rendue sans objet (`698da99`). La matrice trace
    SEC-05 au modèle choisi, à sa situation observée et au contrat de `config.json`, et ajoute AGT-07
    (`30591b6`). Trois campagnes réelles sur le build `698da99` (sources de `main`), configuration de
    Pi du propriétaire, Node 24.21.0, Pi 0.87.1, de 17:16 à 17:27 UTC : local accepté sans annonce,
    trois démarrages `on_machine` ; distant accepté, specify sous omlx `on_machine`, prepare et
    implement sous `anthropic/claude-sonnet-5` `off_machine`, une annonce à l'écran et une sur
    l'entrée structurée, 0,0758 $ par abonnement ; négatif refusé (« policy.egress is no longer
    read »), puis accepté après retrait et rechargement, témoin absent partout. Aucune adresse ni
    aucun jeton dans les dossiers. Écart à la spécification, déclaré dans le relevé : la ligne de
    coût ne nomme pas le modèle Anthropic (elle ne nomme le modèle que quand le coût est inconnu) ; le
    modèle joint est établi par la strate observée dans la requête. Relevé :
    `specs/verifications/e25s04-modele-choisi-admis.md` (`72b5640`). Preflight verte sous Node
    24.21.0 à 17:30 UTC (452 tests). Prochaine étape : `verify-work`, recette à confirmer par le
    propriétaire.

    e25s05 versée sur `main` le 2026-09-24 par avance rapide, sur décision du propriétaire. Preflight
    verte sous Node 24.21.0 (452 tests).

    Étape 6, relecture close le 2026-09-24 : A et B passent le tour 2 sur `6f3010b..0263b18`, sans
    point à corriger. Trois remarques : deux reprises dans `c2c4cc7` (le README dit que le refus nomme
    les trois premiers écarts et compte les autres ; un commentaire de session citait encore une clé
    de configuration ignorée, que la branche avait rendue fausse). La troisième, un repli si la liste
    des écarts était vide, est laissée : le contrat n'a pas d'autre schéma `false` que ses
    `additionalProperties`, et la limite est déclarée dans `e25s05-verify.yaml`. Relevés ancrés à
    `c2c4cc7` : `e25s05-verify.yaml`, `REVIEW.md` (« Reprise en relecture »), l'audit. Preflight verte
    sous Node 24.21.0, 16:53:31Z à 16:55:52Z, 452 tests.

    Étape 6, relecture, tour 1 sur `main...6f3010b` le 2026-09-24 : A et B échouent sur le même point
    à corriger, introduit par la branche. Une valeur qui enfreint deux bornes d'un réglage (`null`
    sur une liste de valeurs permises, `0.5` sous un minimum) comptait pour deux écarts, et au-delà
    du plafond du validateur « at least N more » pouvait être faux. Rouge `91caebc`, observé sur ce
    seul arbre : 2 échecs sur 22 dans `test/v1/config-schema.test.ts`, 1 sur 5 dans
    `test/v3/config-refused.test.ts` sous la copie de TypeBox de Pi. Vert `a94ef1f` : les clés
    inconnues sont lues du fichier seul, sans plafond, et le validateur ne donne plus que les valeurs
    fausses, une par emplacement ; `take()` disparaît, ce qui règle aussi deux remarques (compte
    lâche quand plusieurs sections portent une clé inconnue, section fausse pour deux clés à `~` et
    `/` sous la copie de Pi) et les noms confus. Remarque reprise : le README redit qu'un fichier
    qu'on ne peut ouvrir ou qui n'est pas un fichier ordinaire arrête tout `/495` (`ecf3aba`, vérifié
    dans un vrai Pi pour `/495 help`). Remarque laissée : le test du compte suppose le plafond de 8,
    mesuré à l'exécution. Preflight verte sous Node 24.21.0 (452 tests). Tour 2 : relire
    `6f3010b..ecf3aba`.

    Étape 6 en cours le 2026-09-24. `audit-code` passe sur `770c7a9..57cd3b3`
    (`specs/verifications/AUDIT-e25-e25s05.md`) après deux corrections du nommage des clés inconnues
    dans un refus, sans valeur reproduite ni fichier admis : plusieurs clés non citables d'une section
    comptaient pour un seul écart, et une clé qui contient `/` masquait, sous la copie de TypeBox du
    dépôt, la clé inconnue que son pointeur brut désigne. Rouge `6e5401b` rejoué dans un arbre
    détaché (2 échecs sur 20), vert `57cd3b3`. Le test v3 ajouté sous la copie de Pi échoue sans le
    repli sur le pointeur brut. Preflight verte sous Node 24.21.0 à `57cd3b3` (449 tests). Relevés
    mis à jour : `specs/verifications/e25s05-verify.yaml`, `specs/security/REVIEW.md`. Prochaine
    étape : `request-review` sur `git diff main...HEAD`.

    Étape 5 faite le 2026-09-24, `verify-work` : recette confirmée par le propriétaire. Preflight
    verte sous Node 24.21.0 sur le code de `eb9676c` (446 tests). `security-review` : un constat à
    confiance ≥ 8, corrigé — sous la copie de TypeBox que Pi fournit à l'extension (1.3.27), le
    pointeur d'une clé inconnue n'échappe pas `/`, et une clé `X/y` était citée `X.y`. Test rouge
    `10293ed` dans un vrai Pi, correction `eb9676c` : la clé est lue dans le fichier le long du schéma.
    Sonde de 223 fichiers : 40 fuites avant, 0 après, sous les deux copies. Recette en vrai
    `pi --mode rpc` (`~/.495-campagnes/e25s05-*`) : egress, `Human`, les quatre cas du registre et une
    clé inconnue refusés sans valeur reproduite ; le fichier corrigé par l'exemple du README et
    `/reload-runtime` mène un changement à accepted ; une `baseline` partielle garde la comparaison.
    Contrôles négatifs sur `770c7a9` : la conception passe au noyau sans un mot, `allow_unconfined:
    "no"` est lu vrai, la comparaison est abandonnée. Les deux campagnes complètes ont tourné avant la
    correction ; les refus ont été rejoués dessus. Preuves : `specs/verifications/e25s05-verify.yaml`.
    Prochaine étape : `audit-code`.

    Étape 4 faite le 2026-09-24 : les cinq tâches de e25s05 passent, Preflight verte sous Node
    26.9.0 (445 tests). Chaque rouge a été rejoué par arbre de travail détaché, avec ses seuls tests :
    tâche 1, rouge `cee3829` (le contrat manque, le fichier de test entier échoue), vert `41d3806`
    (8 sur 8) ; tâche 2, rouge `6db4a4f` et `0c55ae7` (9 échecs sur 35), `4b611a2` (8 sur 35), vert
    `c290f69` (35 sur 35) ; tâche 3, `test/v3/config-refused.test.ts` de `6dba049` posé sur
    `4b611a2`, 2 échecs sur 2, vert `6dba049` ; tâches 4 et 5, un seul commit `dff241e`, les trois
    tests de l'exemple posés sur `6dba049`, 3 échecs sur 18, puis 18 sur 18. `4b611a2` ne retire un
    échec qu'en corrigeant le texte qu'attendait un test de `test/v3/pi-rpc-sdk.test.ts`
    (`No program bound`) : ce test couvre le refus d'un fichier illisible, qui existait avant la
    validation. Les `security_verify` des tâches 1 et 2 restent à établir par `security-review` à
    l'étape 5. Prochaine étape : `verify-work`.

    e25s05 tâche 5 ajoutée et passante le 2026-09-24, sur demande du propriétaire : le README montre
    un `config.json` complet aux valeurs par défaut, hors `$schema`, `policy_id` et `revision`, et le
    tableau décrit désormais `policy.baseline`, `policy.stagnation_identical_candidates` et
    `isolation.allow_unconfined`. Trois tests de `test/v1/config-schema.test.ts` lisent l'exemple
    dans le README : le schéma l'accepte, il porte chaque clé que le schéma nomme, et il charge la
    même configuration qu'un fichier vide. Rouge relevé avant l'exemple, 3 échecs sur 3. Contrôles
    négatifs : `max_attempts` à 4, puis une clé retirée, font chacun échouer un test. Preflight verte
    sous Node 26.9.0 (445 tests).

    e25s05 tâche 4 passante le 2026-09-24 : le README dit que `config.json` est validé contre son
    schéma avant tout réglage, pointe vers `contracts/v1/harness-config.json`, et range le refus du
    schéma avec le fichier illisible ; la liste des sections qui ne sont pas des objets est retirée,
    le schéma les couvre. Le refus nomme trois écarts, compte les autres et ne reproduit aucune
    valeur. Preflight verte sous Node 26.9.0 (442 tests). Les quatre tâches passent ; reste à
    consigner le cycle rouge-vert de la story.

    e25s05 tâche 3 passante le 2026-09-24 : `test/v3/config-refused.test.ts`, dans un vrai Pi, refuse
    `/495 start` en JSON et en RPC sur une clé inconnue, puis accepte le fichier corrigé après un
    rechargement. RPC n'a pas de `/reload` : l'extension d'exemple de Pi `reload-runtime.ts` appelle
    `ctx.reload()`, le même `session.reload()` que le `/reload` interactif. Aucun code de production :
    les tâches 1 et 2 portaient le comportement, le test est vert à son premier commit. Rouge relevé
    par arbre détaché sur `4b611a2`, avant la validation : 2 échecs sur 2, un changement entier
    accepté malgré la clé inconnue. Contrôle négatif sans le rechargement : le fichier corrigé reste
    refusé. Reste la tâche 4.

    e25s05 ajoutée à l'epic le 2026-09-24, et placée avant e25s04, sur décision du propriétaire :
    `config.json` est validé contre un schéma JSON strict, et un champ inconnu le fait refuser,
    `policy.egress` compris, qui n'est plus ignorée et annoncée. Étapes 1 et 2 de `build-epic` :
    spécification écrite
    (`e25s05-config-json-est-valide-par-un-schema-et-un-champ-inconnu-le-refuse.md`), quatre tâches
    (`e25s05-tasks.yaml`), validées par le propriétaire le 2026-09-24. Le schéma est un contrat
    TypeBox de `src/contracts/v1/`, émis sous `contracts/v1/` (ADR-006), fermé à chaque niveau ; il
    reprend chaque clé que la lecture accepte, `baseline` est désormais fusionnée sur son défaut, et
    `policy.adoption.protocol` n'admet que `kernel`. Le refus reprend celui du fichier illisible et
    nomme l'emplacement de chaque écart, jamais la valeur. Il ferme le défaut du champ du mauvais
    type ; celui du budget de tentatives reste ouvert. Aucune commande `/495 doctor` : `/reload`
    relance la validation.

    e25s04 révisée en conséquence et validée le 2026-09-24 : la recette tourne sur le
    build qui porte e25s05 ; le contrôle négatif est refusé jusqu'au retrait de la liste, puis
    accepté après `/reload` ; le témoin sous l'ancien code est retiré. `D-61` dit aussi la
    validation par schéma. Les deux spécifications sont versées sur `main`. Prochaine étape :
    `kickoff-branch` pour e25s05.

    e25s03 versée sur `main` le 2026-09-24 par avance rapide, sur décision du propriétaire. Preflight
    verte sous Node 24.21.0 (426 tests).

    Relevés ancrés le 2026-09-24 sur `c17215f`, la révision relue : Preflight verte sous Node
    24.21.0 (426 tests, 10:28 à 10:30 UTC), commandes des tâches rejouées (tâche 3 : 13 tests).
    `e25s03-verify.yaml`, `NFR-e25s03.json`, `AUDIT-e25-e25s03.md` et la section e25s03 de
    `specs/security/REVIEW.md` disent que les campagnes ont tourné sur `db1173c`. Les deux
    comportements changés en relecture ne reposent que sur des tests. Le premier est le choix d'une
    extension chargée avant 495, dit une fois par l'ouverture (`0bf0707`, mesuré dans un vrai
    `pi --mode rpc`). Le second est le libellé « was selected » (`8083cd0`). L'auto-revue note que sa
    dépendance d'ordre a été contredite puis corrigée.

    Étape 6, relecture close le 2026-09-24 : A et B passent les tours 2 à 4, sans point à corriger.
    Tour 2 sur `4a70a09..545a661` : le garde avant l'ouverture ne perd aucune sélection réelle (Pi
    0.87.1 lu), le test de l'extension chargée avant 495 échoue sans lui (2 pour 1). Deux remarques
    intégrées à la demande du propriétaire : le champ `openedSession` dit qu'il retient l'annonce
    jusqu'à l'ouverture, et la spec porte le flux 6l, qui étiquette ce test (`bc61789`, `fefeeac`).
    Tour 3 : 6l disait à tort que 495 n'envoie rien au modèle remplacé, alors que son outil est
    déclaré dès le chargement ; réécrit (`c17215f`). Tour 4 sur ce seul paragraphe : passe. Remarques
    laissées : 6h et l'étape 6 disent « ouverture de session » sans dire que c'est celle de 495 ;
    une sélection non attendue dans `session_start` passe par l'étape 7, annoncée une fois ; le test
    du double démarrage RPC ne porte pas de flux. `8083cd0` seul : Preflight verte (426 tests), sous
    `$HOME`. Preflight verte à `c17215f` (426 tests). Prochaine étape : ancrer les relevés de
    `specs/verifications/` et `specs/security/REVIEW.md` sur la révision relue, puis livrer.

    Étape 6, relecture, tour 1 sur `fadf8f1...4a70a09` le 2026-09-24 : A échoue sur un point à
    corriger, B passe ; aucun point bloquant. Le point, trouvé par les deux : une extension chargée
    avant 495 qui choisit un modèle distant dans son `session_start` fait dire l'annonce deux fois à
    l'écran, parce que `model_select` arrive avant l'ouverture de 495 (`preset.ts` des exemples de
    Pi le fait). Rouge `f9a5b25` dans un vrai `pi --mode rpc` (2 avertissements pour 1), vert
    `0bf0707` : un choix reçu avant l'ouverture n'est pas dit, l'ouverture le lit de `ctx.model`.
    La file remplacée à l'ouverture ne dépend plus de l'ordre des événements. Trois remarques
    traitées à la demande du propriétaire : l'annonce dit « was selected », qui reste vrai quand un
    autre modèle a été choisi depuis (`8083cd0`, spec §7 alignée) ; le commentaire du double
    démarrage ne promet plus la file gardée, et son test vérifie la première annonce ; les étiquettes
    de cas de `model-select.test.ts` nomment leur exigence (`f3fbeb7`). Preflight verte (426 tests).
    Prochaine étape : tour 2 sur `4a70a09..HEAD`.

    Étape 6 en cours le 2026-09-24. `audit-code` passe sur `fadf8f1..ca712bb`
    (`specs/verifications/AUDIT-e25-e25s03.md`). Seul correctif : le commentaire du champ `location`
    se lisait à contresens (`ca712bb`), Preflight verte (425 tests). Trois points laissés et motivés :
    la forme du modèle écrite en ligne à trois endroits du domaine, la file des diagnostics remplacée
    à l'ouverture (sûre tant que `model_select` suit `session_start`), et 6h et 6k sans test.
    Prochaine étape : `request-review` sur `git diff main...HEAD`.

    Étape 5 faite le 2026-09-24, `verify-work` : recette confirmée par le propriétaire. Preflight
    verte à `0816d28` sous Node 24.21.0 (425 tests). `security-review` : aucun
    constat à confiance ≥ 8, les `security_verify` des tâches 1 et 2 établis (`specs/security/REVIEW.md`,
    section e25s03). Quatre exécutions réelles dans `pi --mode rpc` (`~/.495-campagnes/e25s03-*`) :
    `omlx-loin` est le point local nommé `127.0.0.1.nip.io`, fiction déclarée. Campagne acceptée G0..G5,
    démarrages inscrits off_machine puis on_machine, deux annonces, aucune au retour local, adresse
    absente du dossier ; contrôles négatifs sur `fadf8f1` sans annonce ni situation. Print et JSON
    disent l'annonce une fois. Preuves : `specs/verifications/e25s03-verify.yaml`, `NFR-e25s03.json`
    (GO). Prochaine étape : `audit-code`.

    Étape 4 faite le 2026-09-24 : les quatre tâches de e25s03 passent, Preflight verte (425 tests,
    Node 26.9.0). Tâche 3 verte `299626c` : `model_select` écouté dans `src/extension/index.ts`,
    `ExtensionSession.modelSelected` met l'annonce dans la file des diagnostics d'ouverture, et
    `openedAt` juge le modèle actif, runtime créé ou non. Mesuré dans un vrai `pi --mode rpc` :
    le modèle restauré est déjà lisible dans `session_start` (§19 de la spec, fermé). Défaut trouvé
    en sondant : le mode RPC démarre deux fois la même session sur `new_session` (`rpc-mode.js`),
    ce qui disait deux fois l'annonce à l'écran et remplaçait la file non lue. Rouge `9536fc8`
    (vérifié en échec isolé), correctif `db1173c` : un second démarrage du même identifiant de
    session n'est plus une seconde ouverture. Le `security_verify` de la tâche 1 reste à établir par
    `security-review`. Prochaine étape : `verify-work`.

    Tâche 3 rouge le 2026-09-24 : `47bbc95`, vérifié en échec isolé par arbre de travail détaché.
    Trois tests de `test/v3/model-select.test.ts` échouent sur l'absence d'annonce : `/model` vers
    un modèle distant dans un vrai `pi --mode rpc` (retour au local sans annonce, adresse jamais
    dite), session rouverte sur un modèle distant par `--session` sur un fichier écrit à la main
    (Pi n'écrit une session qu'après une réponse du modèle), et runtime refusé (6i) par l'entrée
    `index.ts` sur un faux Pi. Mesuré : Pi 0.87.1 restaure le modèle du fichier (`get_state`).
    Prochaine étape : le vert, `model_select` écouté dans `src/extension/index.ts` et l'annonce
    mise dans la file des diagnostics de `src/extension/session.ts`, y compris quand le runtime
    manque.

    Tâche 2 passante le 2026-09-24 : `selectedModel` (`src/extension/conduct.ts`) porte la
    situation dans `ModelSelection`, que le démarrage d'intervention inscrit tel quel ; champ
    facultatif dans l'événement et l'état pour un dossier ancien, obligatoire dans la commande.
    Rouge `c6b0fa7` (vérifié en échec isolé), vert `75fa550`, Preflight verte (421 tests). Le test
    `pi --mode rpc` de `test/v3/model-select.test.ts` lit `on_machine` pour les modèles du catalogue
    déclarés à `127.0.0.1:9` : la lecture passe par Pi de bout en bout. Prochaine étape : tâche 3,
    test rouge de l'annonce dans `test/v3/model-select.test.ts`.

    Étape 4 en cours le 2026-09-24. Tâche 1 passante : `locateModel` dans `src/domain/policy.ts`
    lit la situation du nom d'hôte de l'adresse, rouge `d6d8744` (vérifié en échec isolé par arbre
    de travail détaché), vert `398c3a3`, Preflight verte (416 tests, Node 26.9.0). Son
    `security_verify` reste à établir par `security-review` à l'étape 5. Prochaine étape : tâche 2,
    test rouge `test/v2/model-location-journal.test.ts`.

    Étape 3 faite le 2026-09-24 : la spécification de e25s03 est versée sur `main` (`fadf8f1`), la
    branche `situation-du-modele-lue-de-son-adresse` part de ce commit, et la Preflight y est verte
    (397 tests, 0 échec, sous Node 26.9.0). Prochaine étape : `develop-tdd`, en commençant par le
    test rouge de `test/v1/model-location.test.ts`.

    e25s03 ouverte le 2026-09-24, étapes 1 et 2 de `build-epic`. La spécification est écrite
    (`e25s03-la-situation-du-modele-est-lue-de-son-adresse-portee-au-dossier-et-annoncee.md`) et
    a été validée par le propriétaire le 2026-09-24. La situation est lue du nom d'hôte de `Model.baseUrl`, sans
    résolution : `localhost`, `127.0.0.0/8` et `::1` sont sur la machine, tout le reste hors d'elle.
    Elle est portée par `ModelSelection`, donc lue dans la même lecture que le modèle jugé, et
    inscrite au démarrage de l'intervention sans l'adresse. L'annonce passe par la file des
    diagnostics d'ouverture, alimentée à `session_start` et à `model_select`. Lu dans le code de Pi
    0.87.1 : `model_select` n'est émis que pour `set` et `cycle`, jamais pour un modèle restauré,
    que `createAgentSession` résout avant `session_start` ; le worker résout le modèle depuis
    `models.json` sans les autres extensions de la session, limite notée au §15. Aucune modélisation
    de menace par epic (étape 0) ni rapport d'impact, comme pour les stories précédentes.
    Prochaine étape : `kickoff-branch`.

    e25s02 versée sur `main` le 2026-09-24 par avance rapide, sur décision du propriétaire, qui a
    confirmé les trois campagnes de reprise après refus. Preflight verte sous Node 24.21.0.

    Étape 6 faite le 2026-09-24. Troisième tour de `request-review`, sur `git diff 8c7a569 612bd09` :
    porte passée pour les deux relecteurs, trois remarques à considérer. Deux sont reprises en
    commentaire dans `af09eeb` (la reprise d'une tentative non démarrée ne vaut qu'avant la première
    tentative, et suppose une référence et une préparation qui ne changent pas d'ici là) ; la
    troisième, un espace de travail supprimé à la main pendant le blocage, a le même trou que la
    reprise d'une tentative ouverte et n'est pas traitée. Relevés ancrés à `af09eeb` : Preflight
    verte sous Node 24.21.0, 397 tests. Les deux comportements ajoutés en relecture sont exercés
    par trois campagnes réelles : sur `af09eeb`, l'implémentation refusée pour Flash-Next reprend
    après `/model` et `/495 resume` dans l'espace déjà préparé, acceptée G0..G5 ; sur `c1ff51c`, la
    reprise ne lève rien ; sur `8c7a569`, elle aboutit mais sur une seconde copie du projet. Relevé :
    `resume_after_refusal` dans `e25s02-verify.yaml`.

    Étape 6 en cours le 2026-09-24. `audit-code` passe sur `952172c..ed4ca34`. Premier tour de
    `request-review` sur la même étendue : porte passée pour les deux relecteurs, aucun must-fix ni
    should-fix imputable à la branche. Le relecteur B a trouvé un défaut antérieur à la branche,
    `BUG-2026-09-24T000204` : un refus de modèle ne se reprenait pas après `/model`. Corrigé par
    `fix-bug` dans cette branche, test rouge `6bd0ba5`, correctif `144d371`, Preflight verte
    (396 tests). Deux remarques du premier tour sont reprises dans le commit de test : le test 6e
    vérifie que le refus vient du modèle, et le lecteur par défaut du fixture ne s'efface plus. Les
    relevés restent ancrés à `c1ff51c` jusqu'à la fermeture de la porte. Second tour, sur
    `git diff ed4ca34 8c7a569` : porte refusée par le relecteur B sur un défaut rendu atteignable,
    une copie du projet laissée par chaque reprise d'une implémentation refusée pour son modèle.
    Corrigé : test rouge `6a3e927`, correctif `91e251e`, Preflight verte (397 tests) ; le refus du
    bac à sable est vérifié non réessayable, et la limite des dossiers bloqués avant le correctif
    est notée au registre. Prochaine étape : troisième tour de `request-review`, sur
    `git diff 8c7a569 91e251e`.

    Étape 5 faite le 2026-09-23 : `verify-work` a vérifié e25s02 sur quatre campagnes réelles en
    `pi --mode rpc`, deux sur la branche et deux contrôles négatifs sur `main`, et le propriétaire a
    confirmé la recette. `e25s02-en-route` est accepté G0..G5 : la spécification démarre sous
    `omlx/qwen3.8-27b-oq8e`, choisi après le remplacement de session, et la préparation et
    l'implémentation sous `omlx-b`, choisi pendant la spécification, qui garde son modèle. Sur
    `main`, la même séquence est refusée sur le modèle de l'ouverture. `e25s02-flash-refuse` montre
    que le modèle sélectionné est celui que juge la sonde de capacité. `omlx-b` est le même modèle
    sous un second nom de fournisseur, et c'est déclaré. Preflight verte sous Node 24.21 à `c1ff51c`
    (395 tests), aucun constat de sécurité à confiance 8 ou plus. Preuves :
    `specs/verifications/e25s02-verify.yaml`, `NFR-e25s02.json`, section e25s02 de
    `specs/security/REVIEW.md`. Prochaine étape : `audit-code`.

    Étape 4 faite le 2026-09-23 : `develop-tdd` a porté les trois tâches de e25s02 au vert. Le test
    rouge `fbb4b2e` (`test/v3/model-select.test.ts`, 6 échecs sur 6 en isolation) couvre 6a, 6b,
    §5/6c, 6d, 6e, et 6g contre un vrai `pi --mode rpc`. `eaba714` fait lire à chaque intervention
    le modèle sélectionné quand elle démarre : `advance` reçoit un lecteur, `conduct` le lit dans le
    contexte de la commande, le runtime ne porte plus de modèle, et la vérification de capacité,
    l'inscription et le mandat reçoivent la même lecture. Preflight vert avec 395 tests. 6g est
    confirmé par l'exécution, mais sa cause n'est pas celle qu'on avait lue dans le code de Pi : une
    extension sonde dans un vrai `pi --mode rpc` montre que `new_session` charge une nouvelle
    instance de 495, avec un catalogue de modèles neuf, et que cette instance reçoit deux fois
    `session_start` sans que son contexte devienne caduc. Le rouge de 6g venait du modèle figé à
    l'ouverture, comme 6a et 6b. Le propriétaire a demandé de traiter ici le catalogue supposé
    resté celui de la première session : la mesure montre qu'il est neuf, donc rien n'est à
    corriger dans le code ; la spécification, la tâche 1, le commentaire de `selectedModel` et le
    test 6g sont corrigés de la prémisse du contexte caduc. Relevé :
    `specs/verifications/e25s02-cycle-rouge-vert.md`. Prochaine étape : `verify-work`.

    Étape 3 faite le 2026-09-23 : la spécification est validée par le propriétaire et versée sur
    `main` (`952172c`), la branche `modele-lu-a-chaque-intervention` part de ce commit, et la
    Preflight y est verte (389 tests, 0 échec). Prochaine étape : `develop-tdd`, en commençant par le
    test rouge de `test/v3/model-select.test.ts`.

    e25s02 ouverte le 2026-09-23, étapes 1 et 2 de `build-epic`. La spécification est écrite
    (`e25s02-le-modele-choisi-par-model-est-celui-de-l-intervention-suivante.md`). La tâche 1 ne
    prescrit plus l'écoute de `model_select` : dans Pi 0.87.1, le contexte remis à l'extension lit
    `model` et `thinkingLevel` à l'appel (`createContext`, `core/extensions/runner.js`), si bien que
    495 lit la sélection au démarrage de chaque intervention sans en tenir de copie. Elle est lue dans
    le contexte de la commande ou de l'outil qui fait avancer le changement : celui de l'ouverture de
    session devient caduc quand Pi remplace la session (`dispose`, `core/agent-session.js`), alors
    que 495 garde le runtime du premier démarrage. La lecture est
    faite une fois et passée à la vérification de capacité, au démarrage inscrit, au contexte et au
    mandat, parce que `requireCapable` attend la description du modèle avant l'inscription : relire
    la sélection après cette attente laisserait joindre un modèle non jugé. `model_select` reste
    nécessaire à e25s03, pour l'annonce. Aucune modélisation de menace par epic n'a été produite
    (étape 0), comme pour les stories précédentes.

    Sur décision du propriétaire le 2026-09-23, la relecture a changé de périmètre : une petite refonte,
    la création unique du runtime, relue par un seul relecteur neuf sur ce qu'elle change seulement.
    `e74a04a` crée le runtime à l'ouverture de session seule (`createRuntimeAt` dans `session.ts`) ;
    les commandes, l'outil, `conduct` et l'abandon du chargeur le lisent par `runtime()` sans pouvoir
    le créer, et la ternaire `this.runtime ? [text] : []` devient deux captures distinctes. Premier
    tour, porte refusée sur deux défauts à corriger : le mode RPC de Pi émet deux fois
    `session_start` sur la même extension (`new_session`, `switch_session`, `fork`, `clone`), si bien
    que la seconde ouverture créait un second runtime et laissait le journal du premier ouvert ; et
    `SCOPE_LATEST.yaml` citait encore `ensureRuntime`. Corrigés dans `5e56368`, test vu rouge d'abord
    sur `e74a04a` (`test/v3/session-start-twice.test.ts`, 2 échecs sur 2) ; le modèle est lu dans la
    capture de la création. Second tour, sur `git diff e74a04a 5e56368` : porte passée, une seule
    remarque à considérer (une ligne repliée de `SCOPE_LATEST.yaml` plus longue que ses voisines),
    corrigée. Relevés ancrés à `5e56368` : `REVIEW.md`, le relevé rouge-vert, `e25s01-verify.yaml`,
    `NFR-e25s01.json`. Preflight vert avec 389 tests sous Node 24.21.0. La fusion attend la décision
    du propriétaire.

    `respond-review` a traité le sixième tour le 2026-09-23. Corrigés dans `0b6539d`, tests vus rouges
    d'abord sur `cc4ae51` : la présence de `config.json` est jugée dans un `try` (`present()` dans
    `config.ts`), si bien qu'un répertoire qui ne se parcourt pas est refusé sans le chemin, et ce qui
    n'est pas un fichier ordinaire (un tube nommé bloquait l'ouverture) est refusé avant d'être
    ouvert ; un runtime qui ne se crée pas pour une autre cause est refusé par son seul code d'erreur
    (`cannotCreate` dans `session.ts`), sans le répertoire de données ; chaque refus nomme
    `/reload`, vérifié dans Pi (`session_start` de raison `reload`, `AgentSession.reload`) ; le test
    RPC vérifie la notification d'échec à l'ouverture (`uiRequests`) et ouvre la session suivante par
    `new_session` dans le même processus ; `test/v2/measure-budgets.test.ts` tient le refus du script
    ; la phrase du README nomme les sections vérifiées et `/495 help`. Trois mutations tombent
    (notification retirée, `config.ts` de `cc4ae51`, refus du script retiré). Inscrit au registre, hors
    de la story qui ne l'a pas introduit : les champs du mauvais type (BUG-2026-09-23T184521, relevé à
    medium/p2) — `adoption.design: "Human"` fait adopter le noyau en silence, `integration_enabled:
    "false"` active l'intégration derrière une autorisation humaine, `allow_unconfined: "no"` fait
    refuser tout rôle qui écrit ; la façon de les traiter reste à trancher. Écartés avec motif : l'état
    en trois cellules (`pending` est une file de textes, et `runtime`/`runtimeFailure` n'ont qu'un
    écrivain, `ensureRuntime`) ; l'autre côté de `this.runtime ? [text] : []` (un journal qui échoue
    après son ouverture, qu'aucune entrée publique n'atteint sans faux) ; `/495 help` refusé (le README
    le dit) ; le libellé `existsSync` de `measure-budgets.ts` (atteint après un `loadConfig` réussi, où
    les deux prédicats s'accordent) ; le double `session_start` de Pi sur `new_session` en RPC (propre à
    Pi, deux notifications à l'écran, rien au modèle). `REVIEW.md`, le registre, le relevé rouge-vert,
    la spécification (6b, scénario, §20), la tâche 1, `e25s01-verify.yaml` et `NFR-e25s01.json` sont à
    jour. Preflight vert avec 387 tests sous Node 24.21.0. Ces corrections n'ont été relues par aucun
    relecteur neuf, et la fusion attend la décision du propriétaire.

    Un sixième tour de `request-review`, au-delà du plafond de cinq, a été conduit le 2026-09-23 sur
    décision du propriétaire, avec deux relecteurs neufs, chacun sur une copie sous `$HOME`, sur
    `cc4ae51` : A 60 %, B 88,9 %, aucun défaut bloquant ; porte refusée pour les deux. Preflight vert
    avec 383 tests chez les deux. Les trois comportements de `2908ec6` (refus tenu pour la session,
    dit une fois par commande, lien pendant refusé) font tomber leurs tests sous mutation. À corriger,
    relevé par les deux : la notification d'échec à l'ouverture (`session.ts`, `ctx.ui.notify(text,
    "error")`) n'est vérifiée par aucun test, et sa suppression passe `pi-rpc-sdk` et `pi-entries`
    (`client.uiRequests` n'est pas lu). Relevé par les deux, à corriger selon A : `runtimeFailure`
    fige pour la session tout échec de `createRuntime`, pas seulement `CONFIGURATION_ERROR` — un
    `mkdirSync` refusé (`EACCES`) reste acquis sans dire qu'il faut une nouvelle session, et son
    message porte au modèle le chemin absolu du répertoire de données (fuite antérieure à la branche).
    À corriger selon A : `lstatSync` n'est pas gardé, donc `loadConfig` appelé directement sur un
    répertoire sans droit de traversée rend `EACCES … lstat '<chemin>/config.json'`, contrairement à
    la docstring de `unreadable()`, à `e25s01-cycle-rouge-vert.md` et à `REVIEW.md`. Relevé par les
    deux, à corriger selon A : aucun champ scalaire n'est vérifié — `adoption.design: "Human"` fait
    décider le noyau en silence (`decide.ts` compare à `"human"`), `integration_enabled: "false"` vaut
    vrai ; `allow_unconfined: "no"` choisit le backend non confiné, non qualifié, donc les rôles qui
    écrivent sont refusés en `CAPABILITY_MISSING` et un diagnostic l'annonce (vérifié dans
    `runtime.ts` et `intervention.ts`) ; ces cas ne sont pas nommés dans BUG-2026-09-23T184521. À
    peser : un `config.json` FIFO bloque l'ouverture sans fin (antérieur ; `lstat` permet de le
    refuser) ; trois cellules nullables (`runtime`, `runtimeFailure`, `pending`) codent un état à
    trois valeurs ; `scripts/measure-budgets.ts` n'a aucun test du refus et juge la présence du
    fichier par `existsSync` ; la branche `this.runtime ? [text] : []` n'est testée que d'un côté ;
    selon B, un `/reload` recrée l'extension (déduit de la doc de Pi, non exécuté), donc « nouvelle
    session » est imprécis, `/495 help` est refusé aussi, et la phrase du README sur « a section that
    is not an object » est ambiguë ; selon A, le test RPC ouvre la nouvelle session dans un nouveau
    processus, et Pi émet deux fois `session_start` sur `new_session` en RPC.

    `respond-review` a traité le cinquième tour le 2026-09-23. Le propriétaire a choisi que la
    réparation d'un `config.json` illisible impose une nouvelle session. Corrigés dans `2908ec6`,
    tests vus rouges d'abord : un runtime qui n'a pas pu être créé reste refusé pour le reste de la
    session (`runtimeFailure` dans `session.ts`), et son échec n'est plus dit deux fois avant la
    réponse de la commande ; un `config.json` lien symbolique pendant est refusé (`lstatSync` au lieu
    de `existsSync`) ; chaque corps refusé porte un marqueur, et chaque refus est vérifié sans le
    marqueur ni le chemin (les deux mutations passées au cinquième tour tombent) ;
    `scripts/measure-budgets.ts` rend le refus par `fail()` et ne filtre plus un préfixe disparu ; le
    README dit qu'un fichier illisible bloque tout `/495` jusqu'à une nouvelle session. Une session
    RPC réelle (`test/v3/pi-rpc-sdk.test.ts`) tient la règle. `REVIEW.md`, le registre (dont
    `policy.baseline` ajouté à BUG-2026-09-23T184521), le relevé rouge-vert, la spécification (6b,
    scénario, §20), la tâche 1, `e25s01-verify.yaml` et `NFR-e25s01.json` sont à jour. Écarté avec
    motif : `retryable: false`, désormais exact puisque le refus n'est pas levé dans la session.
    Preflight vert avec 383 tests sous Node 24. Plafond de relecture atteint : ces corrections n'ont
    pas été relues par un relecteur neuf, et la fusion attend la décision du propriétaire.

    Le cinquième et dernier tour de `request-review` (5 sur 5) a été conduit le 2026-09-23 avec deux
    relecteurs neufs, sur `5e43a7d` : A 77,8 % avec un défaut bloquant, B 88,2 % sans ; porte refusée
    pour les deux. Plafond de relecture atteint : la décision revient au propriétaire, et la branche
    n'est pas fusionnée. Preflight vert avec 382 tests chez les deux. Relevé par les deux : après
    réparation d'un `config.json` illisible en cours de session, la commande suivante crée le runtime
    sans résoudre la liaison de la session ni annoncer ses diagnostics — seul `openedAt` le fait —,
    donc `/495 status` dit la session non liée, la garde « déjà liée » de `start` est contournée, et
    `HARNESS495_ALLOW_UNCONFINED` ou `policy.egress` ne sont jamais annoncés (A : bloquant ; B : à
    corriger) ; un `config.json` lien symbolique pendant est lu comme absent (`existsSync` suit le
    lien) et charge les défauts sans rien dire ; aucun test ne ferait tomber un refus qui recopie la
    valeur d'une section, ni le chemin hors du cas `EACCES` ; `REVIEW.md` garde une observation qui dit
    une section non-objet étalée caractère par caractère. Selon A : `scripts/measure-budgets.ts` filtre
    un préfixe « config.json ignored » qui n'existe plus et n'attrape pas le refus. À peser : le README
    ne dit pas qu'un fichier illisible bloque tout `/495` ; le refus est dit deux fois dans tous les
    modes, pas seulement en texte ; `retryable: false` pour un refus que la réparation lève ;
    `policy.baseline` échappe au contrôle de forme (antérieur) ; `verified_at` précède `bda51c7` ;
    §20 de la spécification omet des fichiers touchés ; le nom `section()` appliqué au fichier entier.

    `respond-review` a traité le quatrième tour le 2026-09-23. Le propriétaire a tranché le même jour
    qu'un `config.json` illisible arrête tout changement au lieu de céder aux réglages par défaut.
    Corrigés dans `bda51c7`, tests vus rouges d'abord : `loadConfig` lève `CONFIGURATION_ERROR` sur un
    fichier qui ne s'ouvre pas, qui n'est pas du JSON, qui n'est pas un objet, ou dont une section
    n'en est pas un ; le runtime ne se crée pas, chaque `/495` répond par ce refus jusqu'à ce que le
    fichier soit réparé ou retiré, et le refus ne cite ni le texte du fichier ni son chemin
    (BUG-2026-09-23T184520, corrigé ; la part « section » de BUG-2026-09-23T184521 aussi, un champ du
    mauvais type reste ouvert). Le test du fichier illisible vérifie le refus et son texte ; un
    fichier sans section `policy` n'annonce rien ; un fichier `null` est nommé `null`. Démarrage à
    froid de l'extension construite rejoué : refus, aucun journal créé. La spécification (6b et son
    scénario), la tâche 1, `REVIEW.md` (périmètre, recherche `grep -rnw`), le relevé rouge-vert,
    `e25s01-verify.yaml` et `NFR-e25s01.json` sont à jour ; la note de confirmation dit que les deux
    démarrages à froid postérieurs à la recette ont été conduits par l'agent ; le propriétaire a
    ensuite confirmé lui-même le refus d'un fichier illisible, en le lançant dans son terminal. Écartés avec motif :
    renommer `test/v1/egress.test.ts` (il tient ce qui peut sortir par le canal du modèle, et sept
    relevés de e23 le citent par ce nom) ; le prédicat de test en double, déjà écarté au second tour.
    Preflight vert avec 382 tests. Le suivant est le cinquième et dernier tour de `request-review`
    (5 sur 5), avec deux relecteurs neufs, chacun sur une copie sous `$HOME` construite avant
    Preflight.

    Le quatrième tour de `request-review` (4 sur 5) a été conduit le 2026-09-23 avec deux relecteurs
    neufs, sur `2a33e36` : A 96,9 %, B 93,75 %, aucun défaut bloquant ; porte refusée pour B.
    Preflight vert avec 382 tests sur la même révision. Chaque relecteur a éprouvé les tests par
    mutation sur une copie de l'arbre. À corriger, relevé par les deux : le test « says an unreadable
    file is ignored, without claiming any intervention is refused » de
    `test/v1/model-admitted.test.ts` ne vérifie pas ce que dit son titre, et un diagnostic qui ajoute
    « so every intervention is refused » passe les 10 tests de la story (critère §17 6b sans
    assertion). À corriger selon B : BUG-2026-09-23T184520 est rendu actif par cette branche — un
    `config.json` illisible fait tourner la session sous la politique par défaut et perd les
    arbitrages humains écrits ; B demande que le propriétaire tranche avant la fusion. À peser : une
    erreur d'accès au fichier (`EACCES`) porte son chemin absolu au contexte du modèle ; `REVIEW.md`
    dit que `grep -rn egress src/` ne rend que `config.ts`, alors que « non-regression » dans
    `src/application/preparation.ts` répond aussi ; `test/v1/egress.test.ts` ne porte plus que
    l'environnement fermé du worker ; un fichier sans section `policy` n'est couvert par aucun test du
    « zéro diagnostic » ; un fichier `null` donne « expected an object, read object » ;
    `confirmed_at` de `e25s01-verify.yaml` et `evaluated_at` de `NFR-e25s01.json` précèdent le
    correctif `dcdd890` qu'ils citent. La copie `mktemp -d` proposée aux relecteurs fait échouer deux
    tests Seatbelt (le bac à sable autorise l'écriture sous le répertoire temporaire) et n'a pas de
    `dist/` : un tour suivant copie sous `$HOME` et construit d'abord. Le suivant est
    `respond-review`, puis le cinquième et dernier tour.

    `respond-review` a traité le troisième tour le 2026-09-23. Corrigés : le diagnostic d'un
    `config.json` illisible ne cite plus le message de `JSON.parse`, qui portait un extrait du fichier
    au modèle de la session (`dcdd890`, test vu rouge d'abord, démarrage à froid de l'extension
    construite rejoué ; BUG-2026-09-23T173000, corrigé) ; les tests v2 admettent `acme-hosted`, que
    rien dans 495 ne nomme, et une liste rétablie sur les fournisseurs connus les fait tomber (3
    échecs sur 4) ; chaque forme malformée de `policy.egress` doit produire mot pour mot le diagnostic
    d'une liste, et une valeur recopiée les fait tomber (`67740c4`) ; le titre du test du modèle sans
    fournisseur, le commentaire de `EGRESS_NO_LONGER_READ` et `REVIEW.md` nomment le contexte du
    modèle et `intervention.started` ; le bloc des commandes du relevé rouge-vert est ancré à
    `67740c4` ; « never told about » ne vaut plus que pour la configuration. `e25s01-verify.yaml`,
    `NFR-e25s01.json` et le périmètre de `REVIEW.md` sont ancrés à `67740c4`. Aucun constat écarté.
    Preflight vert avec 382 tests, changements commités. Le suivant est un quatrième tour de
    `request-review` (4 sur 5) avec deux relecteurs neufs.

    Le troisième tour de `request-review` (3 sur 5) a été conduit le 2026-09-23 avec deux relecteurs
    neufs, sur `0a44577` : A 93,9 %, B 92 %, aucun défaut bloquant ; porte refusée pour les deux.
    Preflight vert avec 381 tests sur la même révision. Chaque relecteur a éprouvé les tests par
    mutation sur une copie de l'arbre. À corriger : le fournisseur `anthropic` des tests v2 est nommé
    dans `src/domain/imposed-layers.ts`, contrairement au commentaire de `CHOSEN`, et une liste
    d'admission rétablie sur les fournisseurs que 495 connaît laisse passer les 9 tests de la story
    (vérifié : aucun test v3 ne passe un autre fournisseur par le superviseur) ; la boucle des formes
    malformées de `test/v1/model-admitted.test.ts` compte le diagnostic sans vérifier qu'il est le
    même que pour une liste ni qu'il ne reproduit pas la valeur, et une recopie de la valeur passe ;
    le diagnostic d'un `config.json` illisible recopie un extrait du fichier par le message d'erreur
    de `JSON.parse`, et `pi.sendMessage` le remet au modèle de la session (`convertToLlm` convertit
    un message `custom` en message `user`) — défaut présent sur `main`, pas encore au registre ; le
    commentaire de `EGRESS_NO_LONGER_READ` et `REVIEW.md` omettent le contexte du modèle parmi les
    lecteurs d'un diagnostic. À peser : le bloc « Commandes des tâches » de
    `e25s01-cycle-rouge-vert.md` annonce 6 tests v1, 382 au total et 10 ajoutés, sans révision (5,
    381 et 9 à `0a44577`) ; `intervention.start` nommé comme l'inscription au journal dans
    `REVIEW.md` et le relevé rouge-vert, au lieu de `intervention.started` ; « never told about »
    dans `e25s01-verify.yaml` ne vaut que pour la configuration ; le titre du test v2 du modèle sans
    fournisseur dit « names it unconfigured », que seul le faux produit. Le suivant est
    `respond-review`, puis un quatrième tour.

    `respond-review` a traité le second tour le 2026-09-23. Corrigés : le commentaire de
    `EGRESS_NO_LONGER_READ` et un message d'assertion ne disent plus qu'un diagnostic atteint le
    dossier ; `test/v3/pi-entries.test.ts` porte le diagnostic d'une clé `policy.egress` ignorée dans
    les entrées texte et JSON (contrôle négatif : retirer l'émission le fait échouer) ; l'assertion
    sur la politique du noyau, que le type garantit, est retirée ; `RefusingSandbox` devient
    `UndrivenSandbox` ; `e25s01-verify.yaml`, `NFR-e25s01.json`, le relevé rouge-vert et le périmètre
    de `REVIEW.md` sont ancrés à `3fbe0ea` ; les phrases qui disaient un défaut antérieur à la story
    renvoient à sa fiche au registre. Écartés, avec motif : le prédicat de test en double (un test qui
    écrit lui-même le libellé attendu en reste la spécification) et les tableaux par défaut partagés
    (aucun mutateur, aucun désaccord observable). Preflight vert avec 381 tests, changements commités.
    Le suivant est un troisième tour de `request-review` (3 sur 5) avec deux relecteurs neufs.

    Le second tour de `request-review` (2 sur 5) a été conduit le 2026-09-23 avec deux relecteurs neufs,
    sur `5ec167c` : A 96,3 %, B 91,7 %, aucun défaut bloquant ; porte refusée pour B. Preflight vert
    avec 382 tests sur la même révision. À corriger : le commentaire de `EGRESS_NO_LONGER_READ` dans
    `src/extension/config.ts`, et le message d'assertion de `test/v1/model-admitted.test.ts`, disent
    qu'un diagnostic atteint le dossier exporté par un détail de blocage, ce qui est faux (vérifié :
    rien sous `src/export/` ni `src/application/` ne lit un diagnostic de session) ;
    `e25s01-verify.yaml` et le périmètre de `specs/security/REVIEW.md` restent ancrés à `2d4a615`,
    avant quatre commits qui touchent le code ou les tests ; `REVIEW.md` dit d'un défaut qu'il
    « précède cette story » sans renvoyer à BUG-2026-09-23T184521, ni à BUG-2026-09-23T155707 pour
    `max_attempts`. À peser : `e25s01-cycle-rouge-vert.md` renvoie encore à e25s04 la campagne sans
    configuration que la recette a tenue ; aucun test ne porte le diagnostic d'une clé ignorée dans
    les entrées Pi (§16) ; prédicat de test dupliqué entre v1 et v2 ; `RefusingSandbox` mal nommé ;
    un message d'assertion qui parle de l'ancien README ; une assertion que le compilateur garantit
    déjà ; des tableaux par défaut partagés par référence (aucun mutateur aujourd'hui). Le suivant est
    `respond-review`, puis un troisième tour.

    `respond-review` a traité le premier tour de `request-review` le 2026-09-23 (A 96,6 %, B 89,7 %,
    aucun défaut bloquant ; porte refusée pour B). Corrigés : deux commentaires qui décrivaient encore
    le jugement de la destination, les écrits de recette et le §8 de la spec qui gardaient
    `EgressLocation`, la lecture de `policy` dans `config.ts` depuis une seule source, un faux agent
    nommé et des assertions de test resserrées. Le propriétaire a choisi de consigner sans trancher
    deux défauts de `config.json`, ouverts au registre : un fichier illisible retire l'arbitrage humain
    écrit, et une section qui n'est pas un objet s'étale dans la politique. Laissés : l'archive de
    traçabilité et D-53, prévus par e25s04. Preflight vert avec 382 tests, changements commités. Le suivant est
    un second tour de `request-review` avec deux relecteurs neufs.

    Les changements de `audit-code` et de la recette de `e25s01` sont commités le 2026-09-23, Preflight
    vert avec 382 tests juste avant. Le suivant est `request-review`, pour un second avis sur la
    branche `le-modele-choisi-est-admis`, non fusionnée.

    `audit-code` est passé sur `e25s01` le 2026-09-23 : un seul défaut, le type `EgressLocation`
    resté exporté dans `src/domain/policy.ts` sans aucun lecteur, que `lint:exports` ne voit pas
    parce qu'il ne lit que les fonctions et les constantes. Il est retiré, `dist/` reconstruit,
    Preflight vert avec 382 tests.

    `e25s01` est VÉRIFIÉE et marquée faite, recette confirmée par le propriétaire le 2026-09-23 sur
    trois campagnes réelles (`~/.495-campagnes/e25s01-*`) : sans configuration, sous une liste héritée
    ne nommant que `omlx`, et sous une clé malformée, le fournisseur `anthropic` servi localement est
    admis, six portes au vert, et la clé ignorée est annoncée une fois sans être recopiée. Preuve :
    `specs/verifications/e25s01-verify.yaml` et `NFR-e25s01.json`, revue dans
    `specs/security/REVIEW.md`. La recette a relevé un défaut antérieur, hors du périmètre de la
    story : `policy.budgets.max_attempts` est lu mais n'atteint jamais un changement, qui s'ouvre
    toujours à 3 tentatives, alors que le README le dit réglable (`specs/bugs/registry.yaml`,
    BUG-2026-09-23T155707, ouvert). Le suivant est `audit-code` sur la branche, non fusionnée.

    `e25` EST ENGAGÉ sur décision du propriétaire du 2026-09-23 ; sa capsule est découpée en quatre
    stories (14 points). `e25s01` a ses quatre tâches passantes sur la branche
    `le-modele-choisi-est-admis`, avec Preflight vert et 382 tests. La politique ne porte plus de liste
    de destinations. Une clé `policy.egress` restée dans `config.json` est ignorée et annoncée à
    l'ouverture, et le README démarre en deux étapes. Le cycle rouge-vert et la revue de sécurité sont
    dans `specs/verifications/e25s01-cycle-rouge-vert.md`. Le suivant est `verify-work` sur elle ;
    la branche n'est pas fusionnée.
    « Le modèle choisi dans Pi » désigne le modèle sélectionné au démarrage de l'intervention :
    AGT-07 l'exige, et le premier usage passe par `/model` avant `/495 start` (motif écrit dans la
    capsule, clé `sens_du_modele_choisi`). Le propriétaire a tranché le même jour que `config.json`
    ne porte plus aucune configuration de modèle : `policy.egress` est retirée, pas rendue
    facultative, et D-53 devient sans objet. `main` est poussé sur `origin/main` jusqu'à `e0dc925`, qui porte le README réécrit, Pi
    épinglé en 0.87.1 et le package en 0.2.0. La 0.1.0 est publiée sur npm et étiquetée `v0.1.0` ;
    la 0.2.0 n'est ni publiée ni étiquetée, et sa publication revient au propriétaire (2FA).

    `e23s06` a clos `e23` avec ses huit tâches passantes et Preflight vert sur Node 24.21, avec 393
    tests. Le relevé est `specs/verifications/e23s06-strate-observee.md`, la décision `D-60`, et la
    ligne CTX-02 de la matrice porte les preuves. La branche `controle-du-bloc-fournisseur` est
    supprimée ; le contrôle qu'elle portait reste dans l'historique, au commit `3b1c260`.

    CE QUI EST ÉTABLI. Le worker observe chaque requête par `before_provider_request`, sans la
    modifier. Il sépare ce que le fournisseur a écrit autour de l'invite de l'hôte de ce que l'hôte
    a ajouté aux instructions de 495. La fin de chaque intervention porte, au journal, chaque
    observation qui diffère de la précédente et sa comparaison avec le manifeste. Trois campagnes
    réelles sont gardées, sur la même cible et le même `dist/` (`sha256:ede62b19…`).
    `e23s06-distant` : sur le vrai chemin d'abonnement, le bloc d'Anthropic est observé et concorde
    à chaque intervention, pour 0,0828 $. `e23s06-local` : rien d'imposé, et la concordance.
    `e23s06-negatif` : un fournisseur déclaré, joint sans ce chemin, donne l'écart nommé avec sa
    condition, et le changement va tout de même à son verdict.

    UN FAIT TROUVÉ, À TRANCHER PAR LE PROPRIÉTAIRE. Pi termine l'invite de 495 par une section
    `<cwd>`, qui porte le chemin absolu de l'espace de travail, et il l'envoie au fournisseur à
    chaque requête. Le dossier le dit désormais. Rien ne l'empêche : dire si ce chemin doit quitter
    la machine est une décision sur les données qui sortent.

    CE QUI RESTE OUVERT, CONSTATÉ SANS ÊTRE CORRIGÉ.
    1. La borne de durée n'a d'effet décidé que pour le producteur (`D-19`).
    2. Le débit du modèle local varie de part et d'autre de la bascule, et `D-59` ne décrit que 4,6.
    3. Une relance sans origine humaine établie est encore inscrite au nom du noyau.
    4. Le journal ne garde pas les bornes en vigueur.
    5. Les appels lancés dans le même lot que celui qui dépasse la borne finissent aussi en erreur.

metrics:
  story_start: "2026-09-25T17:07:59+02:00"
